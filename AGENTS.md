# 9ZONE HOMEBOUND — 개발 작업 규칙

이 저장소에서 기능 추가/수정/리팩터링을 할 때는 **영향도 분석(Impact Analysis)을 구현보다 먼저 수행한다.**

## 0. 최우선 원칙

기능이 정상 동작하는 것만으로 완료가 아니다. **기존 화면·입력·게임 규칙·저장 데이터·모바일 레이아웃을 망가뜨리지 않았다는 근거까지 있어야 완료**다.

최근의 대표 회귀 사례처럼, 전투 가로모드용 `overflow:hidden`이 경로 화면 스크롤까지 잠그는 식의 사이드이펙트를 막는 것이 이 규칙의 목적이다.

## 1. 구현 전에 반드시 영향도 분석

코드를 수정하기 전에 변경점이 닿을 수 있는 영역을 아래 9개 축으로 확인한다.

1. **UI / 레이아웃** — 크기, 위치, z-index, 모달, 고정/유동 레이아웃
2. **입력 / 제스처** — 탭, 드래그, 스와이프, 스크롤, pointer/touch 이벤트 충돌
3. **게임 로직 / 밸런스** — 확률, 피해, 카드 소비, 턴/투구 진행, 상태 전이
4. **저장 / 불러오기** — save schema, 이전 세이브 호환, resume 흐름
5. **모바일 뷰포트** — 가로/세로, 작은 높이, safe area, 브라우저 UI 포함
6. **스크롤 / 오버플로** — `overflow`, `height:100vh/100dvh`, fixed/sticky, nested scroll
7. **기존 사용자 플로우** — 타이틀 → 경로 → 전투 → 보상 → 시설 → 다음 경로
8. **테스트 / 회귀** — 기존 테스트가 다루는 계약, 새로 추가해야 할 회귀 테스트
9. **빌드 / 배포** — Vite build, GitHub Pages, 캐시/경로/asset 영향

각 항목은 **영향 있음 / 영향 없음 / 확인 필요** 중 하나로 판단하고, 영향 있음 또는 확인 필요라면 대응 방법을 먼저 적는다.

## 2. 구현 중 규칙

- 전역 CSS, 전역 이벤트, 저장 스키마, 공용 엔진 함수는 **고위험 변경**으로 취급한다.
- 한 화면을 위해 `html`, `body`, `#root`, `.duel-app` 같은 전역 셀렉터를 바꾸는 경우 다른 화면을 반드시 재검증한다.
- 입력 방식을 추가하면 기존 탭/스크롤/스와이프와의 충돌을 확인한다.
- 게임 수치를 바꾸면 표시 UI와 실제 엔진 계산이 같은 값을 쓰는지 확인한다.
- 새 기능은 가능하면 범위를 좁혀 구현하고, 전역 override보다 화면/상태 단위 selector를 우선한다.

## 3. 구현 후 필수 회귀 검증

코드 변경 PR은 최소한 아래를 다시 확인한다.

- 타이틀/메뉴 진입
- 경로 선택 및 세로 스크롤
- 전투 진입 및 9존 조작
- 카드 선택/스와이프/스택 조작
- 투구 결과 및 투수 HP
- 보상 화면
- 시설 화면
- 다음 경로 전환
- 모바일 가로/세로 회전
- 작은 가로 높이에서 잘림/스크롤
- 저장 후 이어하기
- 전체 테스트 + smoke report + production build

변경과 직접 관련된 항목은 반드시 **회귀 테스트를 추가**한다. 재현된 버그는 가능하면 먼저 실패하는 테스트로 고정한 뒤 수정한다.

## 4. PR 영향도 분석은 필수

`.github/pull_request_template.md`의 `IMPACT_ANALYSIS_REQUIRED` 섹션을 삭제하지 않는다.

코드 변경 PR은 다음이 모두 충족되어야 한다.

- 영향받는 화면/시스템을 구체적으로 적음
- 예상 가능한 사이드이펙트를 적음
- 대응/격리 전략을 적음
- 회귀 검증 항목을 적음
- 필수 영향도 체크 10개를 모두 완료함

`.github/workflows/impact-analysis.yml`이 이 계약을 자동 검증한다. 체크가 빠진 코드 변경 PR은 merge 대상이 아니다.

## 5. 완료 정의

**구현 완료 = 기능 동작 + 영향도 분석 + 관련 회귀 테스트 + 전체 테스트 + production build + 배포 후 확인.**

기능을 빨리 넣는 것보다, 기능 하나 때문에 기존 플레이가 깨지지 않는 것이 우선이다.
