# V13 BALLPARK — 방향 확정 · 2026-09-25

이름: 처음엔 "야간경기(NIGHT GAME)"였다. 경기장을 밝게 쓰기로 하면서(규칙 2) "볼파크"로 바꿨다 — 경기장이 곧 화면이라는 뜻. 코드 이름 `BallparkBattle`, `ballpark.css`(`.bp-*`), 플래그 `?park=1`(옛 `?park=1`·`9zone-ng`도 받아 옮긴다).

시안: https://claude.ai/artifact/89BnSGR6YvrU47Nr3u8YVz (소스 `ballpark-mock.tpl.html`, 캡처 `mock-*.png`).
사용자 판정: "이 방향 좋아" → "좋아 이 느낌으로 가자" (2026-09-25).

## 왜 바꾸나

- 픽셀 조정(12px 하한, 겹침 수정)으로는 "별로"가 해결되지 않았다. 문제는 디자인 방향이었다.
- 전투 화면에 박스 9개와 설명문 12줄이 경기장 그림을 덮는다. 강조색이 없고, 그림체(SD 캐릭터 · 칠한 경기장 · 웹 패널)가 서로 다른 게임처럼 보인다.
- 문구가 규칙 설명서처럼 길고 현학적이다.
- 기준: 아르티스 임팩트처럼 장면 하나가 화면이 되는 도트 게임.

## 여섯 가지 규칙 (모든 화면)

1. **장면이 먼저.** 위아래 검은 띠 안에 경기장 한 장면. 정보 패널은 장면 위에 올리지 않고 띠로 내린다.
2. **경기장은 밝게** (사용자 2026-09-25: "캐릭터가 밝은 건 밝은 경기장을 쓰면 되지"). 밝은 SD 캐릭터를 어둡게 누르지 않고, 경기장 원본 밝기를 쓴다. 장면 위 글자는 먹색 받침(`#07080dcc`) 위에 둔다. 어두운 건 위아래 띠뿐.
3. **강조색은 하나.** 조명 노랑 `#ffc861`만. 노리는 칸, 휘두르기 버튼, 볼카운트 불빛, 갈 수 있는 지도 칸. 나머지는 남색 `#141a33` · 먹색 `#07080d` · 뼈색 `#ece4cf` · 청회색 `#7b84a6`.
4. **UI는 그림 속에.** 9존은 중계 화면처럼 타석 앞에 뜬 스트라이크 존. 투수 HP는 머리 위 눈금 12칸. 볼카운트는 구석의 작은 불빛.
5. **행동은 동사 두 개.** "휘두른다" / "지켜본다". 효율·커버 계산은 버튼 아래 한 줄. 원문 규칙은 카드 상세 시트.
6. **설명하지 말고 말하기.** 짧은 해설자 톤. 불확실함은 "확정 예고 아님" 같은 단서 대신 속삭이는 글씨체(기울임)로.

글꼴: 제목 Black Han Sans, 본문 Gowun Dodum, 숫자·라벨 Silkscreen.

## 지도

투수는 검은 실루엣, 갈 수 있는 곳만 노랗게 빛난다. 시설은 7×7 도트 아이콘. 누르면 아래(휴대폰) / 오른쪽(PC)에 컬러 초상화 · 한 줄 성격 · HP 눈금 · 보상 한 줄. 2·3막은 한 줄 예고.

## 미정

- 말투: 해설자 톤(시안) vs 코치 반말 톤.
- ~~캐릭터 밝기~~ → 밝은 경기장으로 해결(규칙 2).

## 구현 계획

기존 전투 화면은 CSS 30여 개 층이 `.duel-combat`을 덮어쓰고, 카드 배치는 DOM 스크립트(`stack-direct-tap.js`)가 숨은 React UI를 대신 누르는 구조다. 이 위에 또 덮어쓰지 않는다. **새 컴포넌트를 옆에 세우고 플래그로 켠다.**

| 단계 | 내용 | 기본값 |
|---|---|---|
| BP-1 | `BallparkBattle.jsx` + `ballpark.css`: 전투 결정 화면(카드 선택 · 존 배치 · 지원 카드 · 준비 카드 · 휘두른다/지켜본다). 엔진 API(`setAimZone`, `previewV10Stack`, `playV10Action`)를 직접 부른다. 투구 연출·결과는 기존 화면 | `?park=1`일 때만 |
| BP-2 | 투구 결과를 장면 안에서(HP 눈금 감소, 판정 한 단어, 다음 공) | 〃 |
| BP-3 | 지도 | 〃 |
| BP-4 | 보상 · 시설 · 타이틀, 문구 전면 교체 | 〃 |
| BP-5 | 기본값 전환, 옛 층 정리 | 사용자 확인 후 |

## BP-1 영향도 분석

| 축 | 판단 | 대응 |
|---|---|---|
| UI / 레이아웃 | 영향 있음(플래그 켰을 때만) | 모든 셀렉터를 `.bp-battle` 아래로. 전역 셀렉터 없음 |
| 입력 / 제스처 | 영향 있음(플래그) | 새 화면은 탭만 쓴다. 옛 DOM 입력 스크립트는 `.duel-combat`을 찾으므로 새 화면에 붙지 않는다 |
| 게임 로직 / 밸런스 | 영향 없음 | 엔진 수정 없음. 옛 화면과 같은 API · 같은 인자 |
| 저장 / 불러오기 | 영향 없음 | 저장은 기존 `persist` 그대로 |
| 모바일 뷰포트 | 영향 있음(플래그) | 세로 / 짧은 가로 / PC 세 배치를 CSS로. 5개 크기 캡처 |
| 스크롤 / 오버플로 | 확인 필요 | 한 화면 고정 없이 문서 흐름. `html/body` 건드리지 않음 |
| 기존 사용자 플로우 | 영향 없음(기본값 꺼짐) | 투구 중·결과는 옛 화면 그대로 |
| 테스트 / 회귀 | 영향 있음 | `tests/ballpark-battle.test.jsx` 추가, 전체 테스트 |
| 빌드 / 배포 | 영향 있음(작음) | Google Fonts 링크를 `.bp-battle`이 쓰는 글꼴만. 빌드 확인 |

## BP-1 기록 · 2026-09-25

- `src/duel/BallparkBattle.jsx`, `ballpark.css`(모두 `.bp-*`), `ballpark-copy.js`. App은 `?park=1`(기억됨, `?ng=0`으로 끔)일 때 `phase==='battle'`·연출 없음에서만 새 화면을 그린다. 투구 연출·결과는 옛 화면.
- 입력: 카드 → 칸 = 메인(엔진 `setAimZone`), 메인이 있을 때 다른 카드 → 칸 = 지원(②③④, `swingStack`), 메인 다시 누르면 비움. 준비 카드는 원형 토큰, 버튼이 "준비한다"로 바뀜.
- 9존 칸 밝기 = 투수가 던질 몫(`publicProbabilities`), 던지지 않는 코스는 빗금.
- 글꼴은 `index.html`의 Google Fonts 링크(전역 로드, 쓰는 곳은 `.bp-battle`만). 이 컨테이너는 외부 글꼴이 막혀 캡처에는 대체 글꼴이 보인다.
- 검증: `tests/ballpark-battle.test.jsx` 10개(플래그 꺼짐=옛 화면, 카드→칸 저장, 지원 ②, 비우기, 휘두르기/지켜보기 1구, CSS 범위), 전체 612/613(실패 1 = 얕은 clone의 V12 baseline SHA, 코드 무관), build, zone report. 실제 브라우저 390×844 · 844×390 · 1440×900에서 2장 스택 → 투구 → 다음 공 → 새 화면 복귀, 오류 0. 플래그 꺼진 옛 흐름 스모크 통과.
- 캡처: `bp1-{390x844,844x390,1440x900}.png`.
- 다음: BP-2 투구 결과를 장면 안에서.

## BP-2 기록 · 2026-09-25 — 투구 결과를 장면 안에서

- 플래그가 켜져 있으면 투구 연출·결과(`pitch`/`between`)·준비 카드 연출·스택 해결까지 볼파크 화면이 그린다. 옛 전투 화면으로 넘어가지 않는다. 마지막 공으로 투수가 내려가면 연출 뒤 보상 화면.
- 투수·타자는 옛 화면과 같은 애니메이션 컴포넌트(`PitcherAtlasSprite`, `Sprite`)를 그대로 받아, 와인드업·스윙 모션이 장면 안에서 돈다.
- 공: 투수 글러브에서 실제 공이 온 칸(존 밖이면 존 옆)으로 날아가 **임팩트 순간**(`timeline.impactAt`)에 떨어진다. 착지 전엔 HP 숫자·눈금이 그대로, 착지 후 눈금이 붉게 깎이고 `-N`이 뜬다.
- 판정: `presentationFor`의 한 단어 제목("한 칸 차이", "넘겼다")을 크게, 그 위에 야구 판정("헛스윙"), 아래 줄에 이유 한 문장. 좋은 결과만 강조색.
- 판정 자리: 세로 = 투수 왼쪽 하늘, PC = 존 위, 짧은 가로 = 마운드 앞 잔디. HP 태그와 존을 가리지 않는다.
- 버튼: 결과가 나오면 동사 두 개 대신 하나 — "다음 공 / 같은 타자" 또는 "다음 타자 / N번 이름". 연출 중엔 잠김. 손패는 흐리게.
- 검증: 테스트 12개(+2: 같은 화면에서 판정·실제 공·다음 버튼, 옛 플래그 이름 이전), 전체 614/615(실패 1 = 얕은 clone의 V12 baseline SHA), build, zone report. 브라우저 390×844 · 844×390 · 1440×900에서 한 판 끝까지(9구, 오류 0), 투수 HP 1에서 최종타 → 보상 화면. 플래그 꺼진 옛 흐름 스모크 통과.
- 캡처: `bp2-{390x844,844x390,1440x900}-result.png`, `bp2-390x844-impact.png`.
- 남음: 볼카운트 불빛은 착지 전에 이미 바뀐다(엔진이 결과를 먼저 저장). 옛 결과 화면의 상세(이유 사슬 `ResultChain`, 첫 두 타석 해설)는 아직 없다.
- 다음: BP-3 지도.

## 글자 덜기 · 2026-09-25

사용자: "타자 이름은 필요 없어, 투수 이름은 남기고 이런 것처럼 불필요한 텍스트 날려줘." 기준 = 판에 이미 보이는 걸 글로 되풀이하지 않는다.

- 뺌: 타자 번호·이름(상단 줄, 다음 타자 버튼), 상단의 구장 이름("정규 승부" → "1막"만), 장면 속 속삭임(아래 코치 줄과 같은 말), 카드의 "N존 커버"(그림이 이미 보여줌), 맨손 스윙 설명, 휘두르기 버튼의 카드 이름·코스(판에 토큰으로 보임), 결과의 이유 문장(실제 공 위치가 판에 보임), 다음 버튼 보조줄("같은 타자").
- 남김: 투수 이름·HP, 볼카운트, 주자, 존 밖 확률, 코치 한 줄(투수 버릇), 카드 이름과 추가 효과 한 줄("정확 적중 HP +50%"), 피해 배율, 판정(야구 판정 + 한 단어).
- 테스트 +1(타자 이름·속삭임·커버 반복 없음, 투수 이름 있음).

## BP-3 · 2026-09-25 — 지도 + 세로 전투 캐릭터 접지

사용자(실기기 캡처): "지도도 볼파크로 바꿔줘", "세로 화면에 캐릭터가 둥둥 떠다녀서 어색해".

- 원인(떠다님): 세로 장면 높이가 화면마다 달라지는데 투수 발이 장면 42% 높이에 있었다. 경기장 그림의 흙은 64%부터라 투수가 관중석 앞 허공에 섰다.
- 수정: 세로 장면을 3:4로 고정(좁은 휴대폰에서는 손패에 자리를 양보해 줄어듦). 세로 상자는 16:9 경기장보다 좁아 `cover`가 높이 기준으로 맞추므로, 흙 시작선이 어느 폰에서도 장면 64%에 있다. 투수 발 = 67%(흙 위 마운드), 타자는 장면 아래 끝에 붙임. 존은 타자 앞 아래쪽.
- 지도 `BallparkMap.jsx`: 지금 막만 보인다. 투수는 검은 실루엣(초상 이미지를 검게), 지금 갈 수 있는 곳만 노랗게 빛나고 길도 노랗게. 시설은 7×7 도트 아이콘. 누르면 아래(세로) / 오른쪽(가로·PC)에 컬러 초상 · 한 줄 버릇 · HP 눈금(그 막 최강 투수 대비) · 보상 한 줄 · "이 구장으로 간다"/"들른다". 못 가는 곳은 "아직 길이 없다", 지난 곳은 "지나온 곳". 초상을 누르면 기존 초상 크게 보기. 다음 막은 한 줄 예고.
- 입장은 옛 지도와 같은 `v10NodeProblem` → `enterV10Node`.
- 발견·수정: 전투의 가로 `grid-area` 규칙이 `.bp-bar`에 걸려 지도 가로 배치를 깨뜨렸다 → `.bp-battle>` 로 좁힘.
- 검증: `tests/ballpark-map.test.jsx` 5개, 전체 620/621(실패 1 = 얕은 clone의 V12 baseline SHA), build. 브라우저 412×915 · 375×667 · 844×390 · 1440×900에서 지도 → 강적 미리보기(잠김) → 입장 → 전투, 한 판 끝까지(오류 0, 가로 넘침 0, 375×667 전투 한 화면). 플래그 꺼진 옛 흐름 통과.
- 캡처: `bp3-map-{p,l,d}.png`, `bp3-grounded-{412x915,375x667}.png`.
- 다음: 보상 · 시설 · 타이틀.

## BP-4 · 2026-09-25 — 보상 · 시설

사용자: "보상이랑 시설 화면도 볼파크로 바꿔줘".

- `BallparkStop.jsx` 하나가 보상과 시설 네 곳(라커룸 · 타격 훈련 · 장비 상점 · 휴식일)을 그린다. 경기장 머리(보상은 쓰러뜨린 투수 초상, 누르면 크게), 제목 한 단어, 한 줄, 고를 것들, 버튼 두 개.
- 문구: "레드 러시 강판 / 한 장 챙겨 가자." → 챙긴다 · 그냥 간다. 라커룸 "손에 안 붙는 배트는 두고 간다." → 뺀다. 훈련 "한 장을 단련한다." → 단련한다. 상점 "하나만 들일 수 있다." → 들인다. 휴식 → 쉰다. 고를 게 없으면 이유 한 줄 + "지도로".
- 카드 면: 커버 모양 그림 + 이름 + 추가 효과 한 줄(강화는 강화 효과). 준비 카드는 둥근 모서리 + "준비" 표. 유물은 마크 + 이름 + 효과. 라커룸은 같은 카드를 한 번만, "덱에 N장". 고르면 위 막대의 덱 수가 "덱 9 → 10"으로 바뀐다. 라커룸에서 고른 카드는 붉은 줄.
- 이전 화면에 있던 "덱이 어떻게 바뀌는지" 상자(ChoiceOutcome)는 덱 수 표시로 줄였다. 강화 전/후 원문 비교는 카드 상세 시트(덱 보기)에 있다.
- 한 화면 고정: 고를 것이 많으면 그 칸만 스크롤, 버튼은 늘 아래(상위 요소의 overflow 때문에 sticky가 안 먹어 높이 고정으로).
- 엔진 호출은 옛 화면과 같다(`claimV10Reward`, `completeV10UtilityNode`).
- 발견·수정: 전투의 가로 버튼 규칙 `.bp-verbs{grid-area…}`가 보상 화면 버튼까지 옮겨 놓음 → `.bp-battle` 아래로 좁힘. `.stop-*` 규칙 범위 누수 → `.bp-stop` 아래로.
- 검증: `tests/ballpark-stop.test.jsx` 5개(보상 받기 · 건너뛰기, 라커룸 종류별 한 번 · 제거, 빈 시설, 플래그 꺼짐), 전체 625/626(실패 1 = 얕은 clone의 V12 baseline SHA), build. 브라우저 412×915 · 375×667 · 844×390 · 1440×900에서 전투 승리 → 보상 선택 → 지도 → 라커룸 제거 → 지도(오류 0, 가로 넘침 0, 한 화면). 옛 흐름 통과.
- 캡처: `bp4-*.png`.
- 다음: 타이틀, 종료 화면.

## 기본값 전환 · 2026-09-25

사용자: "9-baseball.vercel.app 이거 자꾸 이전 버전으로 나오는데". 원인: 볼파크가 `?park=1`을 켠 기기에서만 켜졌다(새 창·저장값 삭제 시 옛 화면). → 볼파크를 기본으로. `?park=0`이면 옛 화면(기억됨), `?park=1`로 다시 켬. 테스트 빌드(`MODE==='test'`)만 옛 화면이 기본이라 기존 테스트가 옛 화면을 계속 검증한다. 프로덕션 빌드에서 빈 저장소 + 주소만으로 지도·전투가 볼파크로 뜨는 것, `?park=0`이면 옛 화면인 것 확인.

## BP-5 · 2026-09-25 — 첫 외부 플레이 피드백

친구 플레이(PC) 피드백: "세로 일렬 잘 티가 안 남", "빗금 표시 뭔 뜻인지 알 수 없음", "세로 일렬 쳤는데 왜 빗나가냐 → 아 맞은 거구나", "존 밖으로 빠지는 건 뭐냐", 종료 화면 "이상한 옵션이 너무 많은 듯". 전체 인상은 "화면 디자인은 이쪽이 나은 듯".

- 커버: 덮는 칸 전부 진한 노랑 채움 + 노랑 테두리, 나머지 칸은 어둡게. 노린 칸은 더 진하게 + ◇. 지원 카드 칸은 파랑.
- 빗금 → 어두운 칸에 "안 던짐" 글자.
- 판정: 큰 글자는 야구 판정 한 단어(안타 · 장타 · 홈런 · 헛스윙 · 스트라이크 · 파울 · 볼 · 볼넷 · 아웃 · 희생타 · 삼진, 엔진 결과에서), 작은 글자로 연출 제목("정확히 맞혔다", "쫓았다"). 좋은 결과는 안타 · 희생타 · 볼넷만(전엔 '정확히 맞혔다' 안타가 좋은 결과로 안 잡혔다).
- 결과 중 9존이 흐려 보이던 원인 = 앱 전체 `button:disabled{opacity:.33}`. 볼파크 칸은 그대로 보이게.
- 존 밖: "존 밖 36%" → "볼 36%". 결과는 존 옆에 흰 공 + "볼 · 존 밖".
- 종료 화면 `BallparkEnd.jsx`: 제목("쓰리 아웃." / "마지막 마운드까지."), 한 줄(누구에게 막혔나, HP 남은 양), 숫자 셋(지나온 곳 · 안타 · 던진 공), 버튼 둘(다시 도전 · 타이틀). 지나온 길 목록 · 원인 사슬 · 덱 보기 버튼 삭제.
- 검증: 테스트 +5, 전체 631/632(실패 1 = 얕은 clone의 V12 baseline SHA), build, 412×915 · 1440×900 한 런 끝까지 · 보상/라커룸 4크기 재확인.

## BP-6 · 2026-09-25 — 볼(유인구)을 보이게

친구: "유인구고 뭐고 못 알아보겠음". 사용자: "유인구 확률도 보여줘야 하나".

- 원인: 공이 9칸 밖(`zone 9`)으로 가도 화면엔 그 자리가 없었다. 칸 밖은 그냥 경기장이라 무엇을 쫓아간 건지 안 보였다.
- 9칸 둘레에 **볼 띠**(점선 테두리, 라벨 "바깥 띠 = 볼"). 볼은 이 띠에 떨어지고, 떨어지면 띠가 하얗게 켜진다.
- 판정 작은 글자: 볼에 휘두르면 "볼에 손이 나갔다", 지켜보면 "볼을 골라냈다".
- 볼 확률은 한 곳에만: **지켜본다 버튼** 아래 "볼일 확률 36%". 지켜볼지 휘두를지 고르는 순간에 필요한 숫자라서 그 버튼에 붙였다. 존 위의 "볼 36%" 표는 뺐다.
- 테스트 갱신(볼 띠 · 지켜본다 확률 · 존 밖 결과가 띠에 떨어짐), 전체 631/632, build, 412×915 · 1440×900 · 844×390 재생.

## C1 · 2026-09-25 — 정리: 볼파크가 본체

외부 평가("새 기능 중지, Ballpark를 본체로 고정, legacy 부채 정리, 빨간 테스트 0")에 사용자 동의.

- 플래그 삭제: 메인 런(v10)은 늘 볼파크(지도 · 전투 · 보상 · 시설 · 종료). `?park=`와 `9zone-park` 저장값은 더 이상 읽지 않는다. 튜토리얼(v9 덱) 런은 옛 화면 그대로.
- App에서 메인 런 옛 화면 분기 4개 삭제(지도 · 보상 · 시설 · 종료), `RunMap` · `RunEndSummary` · `ChoiceOutcome` import 삭제(파일은 v10-fixture 등이 아직 씀).
- 메인 런 전용이던 CSS 층 10개 삭제: `v12-battle-{desktop,landscape,portrait,short,type}`, `v12-board-cover`, `v12-card-prepare`, `v12-card-swing`, `v12-map`, `v12-result`(모두 `.v12-design` · `.v12-result` · `.v10-run-map-screen`에만 걸려 있었다). `v12-polish.css`는 헤더 · 타이틀 규칙만 남김.
- 옛 화면에만 있던 것 중 핵심 둘을 볼파크로 옮김: **CONNECT 선**(`ZoneLinks`가 `.bp-cell`/`.bp-token`도 잰다 — 연결 실선, 끊김 점선), **임팩트 연출**(PixelVFX 파편 · 섬광 · 홈런 흔들림 · 한 칸 차이 슬로모 표시 "ONE ZONE").
- 테스트: 옛 메인 런 화면만 지키던 스위트 10개 삭제(위 CSS 층 · 옛 준비 카드 · 옛 순서 시트 · 옛 결과/지도 레이아웃). 메인 런 통합 테스트는 볼파크 기준으로 옮김(연출 단계 · 슬로모 · 레드 러시 아틀라스 · 첫 경로 초상 · 카드 상세 포커스 복귀 · CONNECT 선 · 화면 키 · 보상 초상). 테스트 빌드도 이제 볼파크가 기본(분기 없음).
- 빨간 테스트: `v12-baseline`의 SHA 도달성 검사. 그 커밋은 main에 합쳐진 적 없는 V12 브랜치 것이라 main 어디서든 실패했다(그동안 "얕은 clone 때문"이라고 한 설명은 틀렸다). 기록(SHA · 브랜치)은 유지, 도달성 검사만 뺌.
- 결과: 테스트 파일 86/86, 테스트 576/576. build, zone/v9/growth/deck report 통과. 브라우저: 메인 런 412×915 · 375×667 · 844×390 · 1440×900 지도→전투→보상→라커룸, 한 판 끝까지, CONNECT 선 · 임팩트 확인, 튜토리얼은 옛 화면으로 진입.

## BP-7 · 2026-09-25 — 읽을 거리를 보이게 + 1막 난도 낮춤

친구: "그런 걸(유인구) 1라부터 넣지 말라니까", "아무런 정보가 없으면 1/9라 확률 족망겜". 사용자: "세 개 다 진행하고 전체적으로 1막 난이도를 낮추자".

- **칸마다 확률**: 투수가 쓰는 칸에 그 칸으로 올 몫(볼 포함 전체 투구 기준, `publicProbabilities`)을 큰 숫자로, 가장 높은 칸은 노랑 + "최다". 볼 띠 라벨에 "볼 N%". 지켜본다 버튼의 확률은 띠로 옮겨 한 곳만.
- **첫 볼 헛스윙 한 번만 안내**: 코치 줄에 "볼은 참으면 볼넷이 된다. 바깥 띠로 올 것 같으면 지켜본다." (`9zone-hint-chase` 저장).
- **1막 난도(밸런스, engine/run-map)**:
  - 볼 비중: 1막 투수만 `BALL_BY_ACT[1]=.35` (2·3막 1 = 변동 없음). 레드 러시 첫 공 볼 36% → 16%.
  - 투수 HP: 1막 `ACT_ESCALATION[1].hp=-12` → 정규 72→60, 강적 92/100→80/88, 보스 120→108. 2·3막 절대값 그대로(1막 대비 표기만 +16→+28, +36→+48).
- **측정** `scripts/act1-report.js`(신규, 300시드 첫 1막 전투, 새 플레이어 봇 = 가장 높은 칸에 카드 1장, 절대 안 지켜봄):

| | 전 | 후 |
|---|---|---|
| 새 플레이어 승률 | 63.7% | 85.3% |
| 2장 스택 봇 승률 | 71.3% | 93.0% |
| 볼 비중 | 26.9% | 11.4% |
| 전투당 볼 헛스윙 | 2.9 | 1.0 |
| 전투당 투구 | 10.8 | 9.1 |

- 저장 호환: 이미 만든 지도의 투수 HP는 저장값 그대로(스키마 변화 없음). 이어하기 중 1막이면 다음 공부터 볼 비중만 바뀜.
- V12 Before 픽스처 비교에서 밸런스 수치(HP · escalation · intent)는 제외(구조 비교는 유지).
- 검증: 테스트 578/578(+2: 칸 확률 합계·최다 표시, 첫 볼 헛스윙 안내 1회), build, zone/v9/growth/deck/v10 report, 브라우저 412×915 · 844×390 · 1440×900 한 판 + 보상/라커룸.

## C2 · 2026-09-26 — 타자·투수를 PixiJS로 (저장소 에셋 그대로)

사용자: "PixiJS가 퀄리티 향상에 도움 된다는데", "투수랑 타자 둘 다", "이미 저장소 에셋이 있잖아".

- 에셋: 새 그림 없음. 투수는 기존 120프레임 아틀라스(12명), 타자는 기존 핵심 포즈 10장. `codex/actor-animation-runtime-smoothness`의 60프레임 타자 아틀라스는 그 브랜치에서 얼굴·배트가 깨져 이미 폐기된 것이라 쓰지 않았다.
- **떠 있는 픽셀 제거** `scripts/clean-batter-islands.py`: 몸에서 떨어진 조각 중 땅선(y 165) 위에 있는 것만 지움. swing-start 91px · swing-mid 97px · follow-through-early 87px · follow-through-late 92px(떨어진 배트 조각), contact 11px 등. 땅 그림자는 유지. 투수 아틀라스 12개 × 120프레임은 검사 결과 0건이라 손대지 않음. 전(`c2-before-islands.png`).
- **`BallparkActors.jsx`**: 스타디움(CSS) 위 WebGL 캔버스 하나에 두 배우. DOM 배우 상자(`.bp-pitcher`/`.bp-batter`) 위치를 읽어 그대로 서므로 세로·가로·PC 배치는 CSS가 계속 결정. Pixi가 그리기 시작하면 DOM 배우는 숨김(상자는 유지). WebGL 없거나 테스트 DOM이면 안 붙고 DOM 배우 그대로.
  - 한 시계: 투수 프레임 = `redRushFrameAt(t)`, 타자 포즈 = V3 타임라인 — 게임 임팩트 단계와 투수 릴리스(프레임 78–79)·타자 접촉이 같은 순간(브라우저 측정).
  - 느린 프레임에서도 핵심 포즈를 건너뛰지 않음(한 번 그릴 때 한 포즈씩 전진).
  - 스윙 빠른 구간: 직전 포즈 2장 잔상(on twos), 배트 궤적 호.
  - 안타: 히트스톱 90ms(두 배우가 접촉 포즈에서 멈춤), 타자 섬광, 앞발 흙먼지. 스트라이드에도 흙먼지.
  - 두 배우 같은 바닥 그림자, 대기 중 숨쉬기.
  - 픽셀 균일: 2배 이상 확대 시 정수 배율로 스냅(×3.2처럼 픽셀 크기가 들쭉날쭉하던 것 해결).
  - `prefers-reduced-motion`이면 잔상·호·먼지·히트스톱 없음.
- 번들: Pixi는 전투 화면에서 동적 import. `vite.config.js` vendor 그룹에서 Pixi 계열 제외 → 첫 로딩 vendor 140 KB 유지, Pixi는 별도 청크.
- 검증: `tests/ballpark-actors.test.js` 5개(10포즈에 떠 있는 픽셀 0 — 옛 그림에선 실패 확인, WebGL 없으면 미탑재, 지연 로딩·vendor 제외, 한 시계·접촉 정지, DOM 숨김 규칙). 전체 583/583, build, report 5종. 브라우저(소프트웨어 WebGL) 844×390에서 포즈·프레임 타임라인 샘플링, 412×915 · 1440×900 안타 프레임 캡처(`c2-contact-*.png`), 4크기 한 판 · 보상 · 라커룸.
- 남은 한계: 타자는 여전히 10포즈라 포즈 사이는 교체다(잔상·호·정지로 체감만 보완). 근본 해결은 중간 포즈를 새로 그리는 것. 9존이 배우 앞에 있어 스윙 호가 존 뒤로 지나간다(전과 같은 층 순서).

## C3 · 2026-09-26 — 공 비행 (순차 작업 1/4)

- 원인: 게임의 접촉 순간이 투수 릴리스 프레임(79)에 붙어 있어 공이 ~50ms만 날았다. → 공은 프레임 76(1267ms)에 손을 떠나고 `PITCH_FLIGHT_MS=300` 뒤 접촉(`redRushTimeline`). 연출 settle은 타자 팔로스루(접촉+500ms)를 기다림. 한 공이 약 0.24초 길어짐.
- 투수별 공 놓는 점 `src/duel/pitcher-release.json`(`scripts/pitcher-release-points.py`: 릴리스 프레임에서 가슴 높이 가장 왼쪽 점 = 던지는 손). 12명 모두.
- Pixi 공: 손 → 실제 칸 중심(볼이면 존 옆)으로, 다가올수록 커지고 살짝 떠올랐다 떨어지는 호, 잔상 7개, 흰 공 + 붉은 실밥. 안타면 필드로 날아감(홈런 높게, 장타 멀리, 단타 낮게, 금색 잔상), 아니면 포수 쪽으로 지나가며 사라짐. 투구 중엔 배우·공 층이 존 앞(`z-index:4`). DOM 착지 표시는 Pixi 공이 닿은 뒤 180ms. WebGL 없으면 옛 DOM 공.
- 측정: 투수 프레임 76 릴리스 → 93 접촉(약 0.28초), 타자 swing-mid가 접촉에 맞음.
- 검증: 테스트 595/595(+2), build, 4크기 한 판 · 보상 · 라커룸.

## C4 · 2026-09-26 — 투수 한마디

사용자: "유인구에 속았을 때, 헛스윙할 때, 강판될 때 등등 여캐들이 캐릭터성에 맞게 한마디… 매력적이고 고혹적이게".

- `src/duel/pitcher-voice.js`: 12명 × 12순간(등판 · 볼에 속음 · 헛스윙 · 삼진 · 루킹 · 볼넷 · 파울 · 아웃 · 안타 · 장타 · 홈런 · 강판) 대사. 목소리는 원화 설정(`assets/pitcher-mobs-v1/PROMPTS.md`)에서: 레드 러시 = 불같은 도발, 청록 미라주 = 몽환·빠져나가는 조롱, 앰버 싱커 = 느긋한 누나, 아이보리 에이스 = 존댓말 기품, 바이올렛 스팅 = 짧고 쏘는 반말, 로즈 페인트 = 장난스러운 애교, 코발트 임팩트 = 호쾌한 힘, 네온 트릭 = 까부는 마술사, 와인 블러프 = 느릿한 고혹, 에메랄드 타이런트 = 여왕의 명령, 플래티넘 헤일로 = 자애롭고 냉정한 천사, 블랙 이클립스 = 속삭이는 어둠. 선수끼리의 도발 수위, 노골적 표현 없음. 34자 이하.
- 같은 공은 같은 대사(투구 번호로 고름). 볼 · 볼넷 없는 볼은 말 없음.
- 표시: 투수 이름표 아래 따옴표 줄(홈런 · 강판은 붉은 줄). 짧은 가로 화면은 장면 아래 캡션 줄에 "이름 + 대사"(판정 상자와 겹치지 않게).
- 같이 고친 것: 짧은 가로에서 준비 카드가 세로로 늘어나 장면을 누르던 문제(원 70px 고정).
- 검증: `tests/pitcher-voice.test.jsx` 3개(그림 있는 투수 전원 × 12순간, 결정성 · 순간 매핑, 등판 대사 → 볼 헛스윙 대사), 전체 598/598, build, 412×915 · 844×390 · 1440×900 다섯 공.

### BP-8 보상 화면 강판 대사
- 강판된 투수의 knockout 대사(`pitcherLine(artId,'knockout')`)를 초상 옆 말풍선으로 크게 띄운다. 380ms 뒤 튀어나오고 초상은 두 번 쿵쾅(heartbeat).
- 보상 화면만. 시설 화면엔 없음. 짧은 가로에선 안내 한 줄을 숨겨 높이 유지. reduced-motion이면 애니메이션 없음.
- 회귀: tests/ballpark-stop.test.jsx `V13 reward voice`. 412×915 / 844×390 / 1440×900 가로 스크롤 없음 확인.

### BP-9 카메라 연출
- 월드 레이어(.bp-cam: 구장 배경, Pixi 캔버스, Pixi 꺼졌을 때 DOM 배우)만 배트 접점(--cam-x/--cam-y) 기준으로 확대. 존·HUD·판정은 확대 안 함.
- 홈런(big) 1.16→1.2 펀치인 후 복귀, 장타·정타(mid) 1.07→1.09 + 약한 흔들림, 한 칸 차이(near) 슬로 비트에서 1.07 서서히 + 레터박스.
- Pixi 배우 시계: slowmo 단계 0.35배, 이후 2배로 따라잡음(`SLOW_RATE`,`CATCH_RATE`). 히트스톱 유지.
- 복귀 애니메이션은 release+settle에서 같은 이름을 써서 단계 전환 때 재시작 안 함. near 복귀 240ms(release→끝 ≈250ms 안).
- reduced-motion: 카메라·레터박스 끔. 회귀: tests/ballpark-camera.test.jsx. 412×915 / 844×390 확인.

### BP-8b 보상 무대 (유저 피드백: 얼굴 잘림)
- 투수 초상은 contain으로 전신이 박스 안에 들어감(얼굴 안 잘림). 세로: 카드가 다리 앞으로 겹쳐 올라옴. 가로/PC: 구장이 화면 전체, 왼쪽 열에 그녀, 오른쪽에 대사·제목·카드.

### BP-10 투수 가시성 (유저 피드백: 투수·이펙트·대사를 UI가 다 가림)
- 실제 폰(412×743, 브라우저 UI 포함)에서 장면이 357px로 줄어 투수가 74px, HP 태그·대사·존에 가려졌음.
- 세로: HP는 상단 우측 한 줄 띠, 대사는 투수 왼쪽 말풍선(꼬리가 그녀를 가리킴), 투수 31% 크기로 확대, 장면 높이 예약 312px로 조정. `.bp-pcol` 래퍼로 가로/PC 배치는 그대로.
- 412×743 / 360×640 / 390×664 / 412×915 세로 스크롤 없음, 844×390 / 1440×900 변화 없음 확인.

### BP-11 투수 이펙트
- 스트라이드: 앞발 착지 프레임(투수별 `pitcher-stride.json`, 오프라인 `scripts/pitcher-stride-points.py`, 모두 51~52)에 발끝 흙먼지.
- 팔 채찍: 릴리스(76) 8프레임 전~3프레임 후, 직전 두 프레임이 따뜻한 빛으로 잔상 + 머리 뒤→손 스우시, 릴리스 순간 손끝 반짝임.
- 강판: HP 0이 된 공은 연출 끝을 700ms 늘림(`withKnockoutHold`, 임팩트·릴리스 타이밍 불변). 히트스톱 뒤 120ms부터 맞은 순간 포즈로 멈춰 붉게 번쩍 → 흔들림 → 뒤로 기울며 주저앉고 어두워짐(투명 X) → 흙먼지. 이어서 보상 화면.
- reduced-motion: 전부 끔. 회귀: tests/pitcher-effects.test.js.

### BP-12 보상 카드 공개
- 보상 화면만: 카드가 뒷면(사선 무늬 + 9존 문양)으로 도착 → 강판 대사 뒤 0.76s부터 한 장씩 170ms 간격으로 도트식 가로 뒤집기(steps(6) scaleX).
- 시그니처(rarity:'signature') = 레어: 뒤집히는 순간 금빛 폭발 → 은은한 숨쉬기 글로우, '시그니처' 태그, 금빛 바탕.
- fill-mode backwards라 선택 카드의 들림(.on)은 그대로. 시설 화면은 공개 연출 없음. reduced-motion이면 바로 앞면.
- 회귀: tests/ballpark-stop.test.jsx `V13 reward reveal`.
