# STATUS

## 최우선 최신 상태 — HOMEBOUND v2

- 출루 카드의 실제 베이스 잠금, 득점/손패 귀환, 대주자 교환, 주루, 희생타/3아웃 득점 무효, 병살 2아웃, 카운트 거래, 반응형 수비를 기존 src/duel에 구현.
- HP/방어/3에너지를 제거. 12카드/4승부/3보상 유지. 별도 v2 저장으로 이전 세이브 보존.
- 전체 57검사/빌드 성공. 실제 React 클릭 완주와 브라우저 2루타→대주자→득점/귀환, 모바일 배치 확인.
- 최신 docs/DUEL.md 첫 절. 아직 인간 재미/밸런스/지배 전략은 미검증. 아트는 기존 자산 재사용, 완성도 도달 주장 금지.
- 다음: 베이스에 따른 선택 변화 관찰, 홈런/회수/희생의 전략 비교, 카드 이동/귀환 연출. 로컬 변경이며 재배포하지 않았다. 아래 v1 기록은 현행 아님.

## 최우선 최신 상태: DUGOUT / ACT 0

- 기본 앱 src/duel/App.jsx. 확정 의도/12카드/여러 장 연계/4전투/3보상/승패/저장·이어하기 구현.
- 새 도트 구장, 캐릭터 각 3포즈, 개별 atlas bounds, 모바일 손패, 선택적 합성음. 기존 앱·자산·저장 보존.
- 전체 51테스트/빌드 통과. React 클릭 완주 및 브라우저 카드 사용·턴 결과·1280/390px 확인.
- 최신 인수인계 docs/DUEL.md. 원본 생성 프롬프트 assets/duel/README.md.
- 다음: 사용자 플레이 관찰, 연계 발견 검증, 카드별 완성 아트/픽셀 그리드 통일. 30분 런/분기/상점/120고유 프레임은 미구현.
- 사용자가 GitHub 업로드 및 배포 요청. 기존 GitHub Pages 경로로 진행. 자동 개발 루프는 켜지 않는다. 아래 LAST LIGHT 절은 역사 기록이다.

## 최우선 최신 상태 · LAST LIGHT 제로베이스 재설계

- 기본 앱을 `src/reboot/App.jsx`로 교체했다. 이전 게임/엔진/CSS/자산을 import하지 않는다. 기존 파일과 저장 키는 보존했다.
- 완결된 3투수×3공격 이닝 런, 실제 진루/득점, 집중 위험 베팅, 5종 타격 카드, 이닝 성장, 승패·재도전·자동 저장/이어하기가 동작한다.
- 새 야간 구장 이미지를 생성해 로컬 자산으로 포함했다. UI는 새 스포츠 타이포그래피·네이비/라임·투수 리포트·9존/카드 조작판으로 재작성했다.
- 자동 테스트 41개(신규 11개 + 보존 코드 30개) 통과. 실제 React 클릭 테스트가 시드 1118527로 154투구·72타석·3매치 우승과 중간 저장 복원을 통과했다.
- 300개 고정 시드: 패턴 활용 완주 18%, PUBLIC 우선 7.3%, POWER 우선 7.3%, 지켜보기만 0%. 인간 테스트가 아니며 이 숫자를 재미의 증거로 주장하지 않는다.
- 브라우저에서 새 시작/매치/타격/결과와 390px 모바일 카드·관찰 노트를 확인했다. 전체 후반 결과 화면의 픽셀 QA 및 인간 플레이테스트는 남았다.
- 다음 작업: 사람의 한 런을 관찰해 실제 판단 시간·이해가 막히는 지점·패턴 발견 경험을 기록한다. 현재 약 30분은 설계 추정이다.
- 현행 인수인계는 `docs/REBOOT.md`. 아래 NIGHT RUN 기록은 이전 버전이며 현재 구조로 되돌리지 않는다. 이번 작업에서 자율 루프와 원격 배포는 시작하지 않았다.

---

## 최신 상태 · 2026-09-09 (아래 9월 2일 기록보다 우선)

- 사용자 요청에 따라 THE NIGHT RUN을 기존 게임에 통합했다. 6라운드×8타석, 3도전, 5보상, 최종전 승패, 자동 저장/이어하기가 구현됐다.
- 기존 자산·연출·공용 CQ/PQ·PUBLIC/TRUE AI를 재사용한다. 런 타석에는 동료 자동시뮬이 개입하지 않는다.
- BASIC SWING 클릭 차단, 중복 스윙의 카드 소비, 세션 전환 후 지연 투구, 압축 전술의 TRUE 누설을 수정했다.
- 검증: 30/30 자동 테스트, 정책봇 3기준, 프로덕션 빌드 통과. UI 자동 테스트가 48타석·192볼·5보상·우승·중간 저장 복원을 확인한다. 이는 승률/재미의 증명이 아니다.
- 화면: 데스크톱과 390px 모바일 스크린샷 확인, 브라우저 BASIC SWING 입력과 카운트 증가 확인. 모바일은 세로 스크롤이며 전체 후반 화면 픽셀 검수는 남았다.
- 다음 한 가지: 사람 3명의 실제 런 플레이 시간·탈락 라운드·재미·가독성을 수집해 25–35분 목표와 난도를 보정한다. 인간 검증은 아직 없다.
- 실행과 규칙은 README, 상세 변경 지점과 체크리스트는 HANDOFF의 최신 절을 먼저 읽는다. 기존 자율 루프를 이번 세션에서 켜지 않았다.

---

## 이전 기록 · 2026-09-02

> 매 바퀴가 끝날 때 갱신한다. 사실, 검증 결과, 다음 한 가지를 짧게 남긴다.

## 현재 상태

- `docs/DESIGN.md`가 코어 감정, READ → BET → REVEAL → IMPACT 흐름, 공용 엔진·정보 비대칭·인간 검증 원칙과 명시적 금지사항을 제품 기준으로 고정한다.
- CQ/PQ 공용 판정 엔진이 `src/game/showdown-engine.js`로 분리되어 게임 본체와 테스트가 같은 모듈을 import한다.
- 역할 선택 화면에서 성장·정규 이닝을 건너뛰고 즉시 한 타석만 진행하는 `CORE TEST`에 진입할 수 있다.
- 한 타석 종료 뒤 네 가지 재미 검증 질문에 답하면 결과가 브라우저 로컬 저장소에 누적되고 JSON으로 내려받을 수 있다.
- 코어 테스트 질문·결과 생성·로컬 저장소 읽기/추가·JSON 직렬화가 `src/game/core-test-results.js` 순수 모듈로 분리되어 UI와 테스트가 같은 경로를 사용한다.
- Random / MaxProb / Pattern / PowerSpam 네 고정 시드 정책봇이 실제 CQ/PQ와 PUBLIC/TRUE INTENT 공용 모듈을 사용하며 지배전략 회귀를 검사한다.
- POWER는 정확한 READ에서만 PQ 보너스를 받고, 오독 시 더 큰 CQ 대가를 치른다. 실제 POWER 존 카드도 이 경로에 연결되어 있다.
- 프로덕션 빌드는 애플리케이션 코드와 외부 의존성을 별도 청크로 분리해 500 kB 초과 청크 경고 없이 생성된다.
- happy-dom과 Testing Library로 실제 React 화면을 렌더링해 튜토리얼 건너뛰기 → `CORE TEST` 진입 → 한 타석 종료 → 네 문항 저장 → 다음 플레이어 → JSON 다운로드를 클릭 회귀 검사한다.
- 여러 플레이어가 내려받은 누적 JSON은 `pnpm run report:core-test -- <파일...>`로 합칠 수 있으며, 완전한 응답만 중복 없이 집계해 3명 목표와 네 재미 신호를 보고한다.
- CORE TEST 보고서의 중복 판정은 JSON 객체의 키 순서와 무관해, 같은 응답을 재포맷한 누적 파일도 한 번만 집계한다.
- CORE TEST 보고서는 앱이 생성한 version 1 내보내기 스키마 전체를 검증하며, 답변만 넣은 임의 객체나 손상된 시각·투구 기록을 실제 인간 응답으로 세지 않는다.
- CORE TEST 보고서는 최상위·답변·투구 기록의 허용 필드와 실제 네 구종 ID까지 정확히 검사해, 숨은 필드나 앱에 없는 구종이 섞인 JSON을 앱 내보내기로 인정하지 않는다.
- CORE TEST 보고서는 완료된 한 타석에 투구 기록이 최소 1개 있어야 한다고 검사해, `pitchesSeen: []`인 불가능한 수동 응답을 인간 결과로 세지 않는다.
- CORE TEST 로컬 저장소도 보고서와 같은 앱 스키마로 응답을 걸러, 손상된 배열 항목이 화면의 3인 카운트·후속 저장·JSON 다운로드에 포함되지 않는다.
- CORE TEST 초기 저장소 읽기가 실패하면 역할 선택 화면에 접근 가능한 오류와 `다시 불러오기` 경로가 나타나며, 복구 뒤 기존 결과 건수를 되살린다.
- CORE TEST 결과 저장이 실패하면 완료 설문 안에 오류가 즉시 표시되며, 기존 답변과 메모를 유지한 채 다시 저장할 수 있다.
- CORE TEST JSON 내보내기가 실패하면 역할 선택 화면에 오류가 즉시 표시되며, 저장된 결과를 유지한 채 같은 버튼으로 다시 시도할 수 있다.
- CORE TEST JSON 내보내기가 Blob URL 생성 뒤 클릭 단계에서 실패해도 임시 링크와 Blob URL을 정리하고 같은 버튼으로 다시 시도할 수 있다.
- 일반 피드백 저장이 실패하면 열린 모달 안에 접근 가능한 오류가 나타나며, 평점과 입력을 유지한 채 같은 제출 버튼으로 다시 시도할 수 있다.
- 관리자 피드백 불러오기가 실패하면 내보내기 패널 안에 접근 가능한 오류와 `다시 불러오기` 경로가 나타나며, 실패 상태에서는 잘못된 JSON 다운로드를 막는다.
- 관리자 피드백 목록의 개별 항목 하나라도 읽기·파싱에 실패하면 불완전한 JSON을 확정하거나 다운로드하지 않고, 같은 패널에서 전체 목록을 다시 불러온다.
- 관리자 피드백 파일 다운로드가 실패하면 내보내기 패널 안에 접근 가능한 오류가 나타나며, 수집 데이터를 유지하고 임시 링크·Blob URL을 정리한 채 같은 버튼으로 다시 시도할 수 있다.
- 인간 3인 플레이테스트의 실제 응답 수집은 아직 진행하지 않았다.

## 마지막으로 완료한 한 가지

- 새 독립 회차에서 CORE TEST 후반 화면 QA를 한 가지 작업으로 다시 시도했다. 화면 확인 전 검사 통과 상태를 체크포인트 커밋 `4679dbf`로 남기고 로컬 앱을 기동했지만, Browser 스킬의 지정 복구 절차와 단일 가용성 조회 결과가 빈 목록 `[]`여서 화면 QA는 중단했다.

## 검증 결과

- 자동 검사: 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check` 통과.
- 정책 확인: PowerSpam/최선 비파워 TB/PA 1.022, 세 지배전략 기준 모두 통과.
- 이번 바퀴 기준선: 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check` 통과. 화면 확인 전 체크포인트는 `4679dbf`이다.
- 실행 확인: 로컬 앱 `http://localhost:5173/` 기동, HTML 요청 HTTP 200 및 `#root` 마운트 지점 확인.
- 확인하지 못한 것: 로컬 URL의 브라우저 선택이 `No browser is available`로 실패했고, Browser 스킬의 지정 복구 안내를 읽은 뒤 기존 런타임에서 한 번 조회한 사용 가능한 브라우저 목록도 `[]`였다. CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기의 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 않았고 다른 브라우저 자동화로 우회하지 않았다.

## 알려진 문제

- 인간 3인 플레이테스트 결과가 없다.
- 코어 테스트 모드의 한 타석 종료 이후 실제 브라우저 픽셀·레이아웃 확인이 필요하다.

## 다음 한 가지 후보

- 사용 가능한 브라우저에서 자동화된 후반 흐름의 픽셀·레이아웃을 확인한 뒤 인간 3명의 실제 JSON을 수집해 `report:core-test`로 집계한다.

## 마지막 갱신

- 날짜: 2026-09-02
- 커밋: 화면 확인 전 체크포인트 `4679dbf`; 이번 문서 커밋은 Browser 스킬의 복구 절차 및 단일 가용성 조회에서도 중단된 CORE TEST 후반 화면 QA 상태를 기록한다.
