# STATUS

> 매 바퀴가 끝날 때 갱신한다. 사실, 검증 결과, 다음 한 가지를 짧게 남긴다.

## 현재 상태

- CQ/PQ 공용 판정 엔진이 `src/game/showdown-engine.js`로 분리되어 게임 본체와 테스트가 같은 모듈을 import한다.
- 1타자 vs 1투수 코어 테스트의 인간 3인 플레이테스트는 아직 진행하지 않았다.

## 마지막으로 완료한 한 가지

- 게임 본체 안에 있던 `resolveShowdownContact()`를 순수 모듈로 분리하고 기존 수식과 두 실제 호출부를 그대로 유지했다 (`64ef916`).

## 검증 결과

- 자동 검사: `pnpm test` 3/3 통과, `pnpm run build` 통과, `git diff --check` 통과.
- 화면 확인: 로컬 Vite 서버가 `http://localhost:5174/`에서 실행되는 것까지 확인했다.
- 확인하지 못한 것: 사용 가능한 앱 내 브라우저가 없어 스크린샷과 직접 플레이 회귀 확인을 하지 못했다.

## 알려진 문제

- 인간 3인 플레이테스트 결과가 없다.
- 프로덕션 번들에 500 kB 초과 청크 경고가 남아 있다.

## 다음 한 가지 후보

- 1타자 vs 1투수 짧은 코어 테스트 모드로 인간 3인 플레이테스트를 수행하고 네 가지 재미 검증 질문의 답을 기록한다.

## 마지막 갱신

- 날짜: 2026-09-02
- 커밋: `64ef916` (공용 엔진 분리; 이 STATUS 갱신은 후속 문서 커밋)
