# STATUS

> 매 바퀴가 끝날 때 갱신한다. 사실, 검증 결과, 다음 한 가지를 짧게 남긴다.

## 현재 상태

- CQ/PQ 공용 판정 엔진이 `src/game/showdown-engine.js`로 분리되어 게임 본체와 테스트가 같은 모듈을 import한다.
- 역할 선택 화면에서 성장·정규 이닝을 건너뛰고 즉시 한 타석만 진행하는 `CORE TEST`에 진입할 수 있다.
- 한 타석 종료 뒤 네 가지 재미 검증 질문에 답하면 결과가 브라우저 로컬 저장소에 누적되고 JSON으로 내려받을 수 있다.
- 인간 3인 플레이테스트의 실제 응답 수집은 아직 진행하지 않았다.

## 마지막으로 완료한 한 가지

- 정규 경기 규칙과 CQ/PQ 엔진을 바꾸지 않고 1타자 vs 1투수 한 타석 코어 테스트 및 4문항 기록 흐름을 추가했다 (`9bde5d6`).

## 검증 결과

- 자동 검사: `pnpm test` 3/3 통과, `pnpm run build` 통과, `git diff --check` 통과.
- 실행 확인: 로컬 Vite 서버 `http://localhost:5174/`가 HTTP 200과 올바른 문서 제목을 반환했다.
- 확인하지 못한 것: 브라우저 런타임의 사용 가능 목록이 비어 있어 역할 선택 → 한 타석 → 설문 저장 → 다음 플레이어의 클릭 흐름과 스크린샷 QA를 수행하지 못했다.

## 알려진 문제

- 인간 3인 플레이테스트 결과가 없다.
- 코어 테스트 모드의 실제 브라우저 클릭·레이아웃 회귀 확인이 필요하다.
- 프로덕션 번들에 500 kB 초과 청크 경고가 남아 있다.

## 다음 한 가지 후보

- 사용 가능한 브라우저에서 `CORE TEST` 전체 흐름을 먼저 회귀 확인한 뒤 인간 3명이 각각 한 타석을 플레이하고 JSON 결과를 수집한다.

## 마지막 갱신

- 날짜: 2026-09-02
- 커밋: `9bde5d6` (한 타석 코어 테스트; 이 STATUS 갱신은 후속 문서 커밋)
