# FEEDBACK INBOX

> 새 지시는 아래에 적는다. 자율 루프는 이 문서를 가장 먼저 읽는다.
> **한 바퀴에 하나만** 집어서 끝내고, 끝낸 항목은 날짜와 커밋을 붙여 아래로 옮긴다.
> 목록이 비면 새 작업을 지어내지 말고 `docs/STATUS.md`의 "다음 작업"을 본다. 거기도 비면 멈춘다.

## 이번 묶음의 목표

사용자 지시: **전체를 마스터피스 수준으로 끌어올린다.**

무엇이 마스터피스인지에 대한 이 저장소의 기준은 `loop/PROMPT.md` ①과 같다 —
*다른 사람이 봤을 때도 퀄리티가 떨어지지 않는가.* 통과 못 하면 커밋하지 않는다.

**단, 문서에 "마스터피스에 도달했다"고 쓰지 마라.** 이 저장소는 검증되지 않은 것을
검증됐다고 쓰지 않는 규율을 지켜왔다. 품질은 올리되 주장은 올리지 않는다.
미검증 항목은 계속 미검증이라고 적는다.

우선순위는 위에서 아래다. 위쪽이 아래쪽의 전제인 경우가 많다.

## 미처리 지시

### 10. V12 — 제품 전체 UI/UX (문서 검수 완료, 제품 미구현)

[검수 근거](../V12-REVIEW.md) · [기획](../V12-MASTERPIECE.md) ·
[규격](../V12-UI-MASTER-SPEC.md) · [결정 시트](../V12-DECISIONS.md).

2026-09-22 아스트라 검수에서 커버·순서 링크·피해 배율·읽기 등급·CSS 이관·입력 계약을 정정했다.
문서 검수자의 권고는 제품 구현/공개 배포 승인과 다르다. 게임 규칙과 아트 품질 기준은 유지한다.

전 화면이 범위이며 P0~P8은 기획서가 단일 기준이다. P0는 캡처·토큰·검사 기반만,
기존 CSS 일괄 래핑과 대량 important 감축은 하지 않는다.
정보 삭제 전 역할·접근 가능한 이름·실제 동작 검증을 보존한다. data-* 존재 검사로 약화하지 않는다.
접근성과 첫 사용 안내는 각 단계에서 검증하고 후반으로 미루지 않는다.
다음은 확정된 범위에 따른 대표 전투 프로토타입과 사람 검증이다. 기존 시안은 정적 개념 자료다.

### 11. "AI 특유의 패널 디자인"을 걷어낸다 (외부 플레이어 피드백 · 2026-09-24)

출처: 커뮤니티(etoland) 공개 피드백 — "저 에이아이 특유의 패널 디자인도 바꾸셔야겠습니다".
외부 사람이 첫눈에 "AI가 만든 UI"로 읽었다는 뜻이다. `loop/PROMPT.md` ① 기준(다른 사람이 봐도 퀄리티가 떨어지지 않는가)에 직접 걸린다.

현재 `src/duel/*.css` 측정치(2026-09-24):
- `box-shadow` 227 · `border-radius` 220(그중 `50%`/`999px` 알약·원 58) · `linear-gradient` 169 · `blur(` 58 · `backdrop-filter` 16
- 영문 대문자 eyebrow + `·`/`/` 구분 라벨 남발: `READ → BET → REVEAL → IMPACT`, `HOME SAFE / DECK DRAFT`, `01 · 카드 보상`, `LIVE SYNERGY`, `V9.2 · CHOOSE THE OPPONENT` 등
- 둥근 반투명 카드 + 그라데이션 + 글로우 + 블러 조합 = 전형적인 생성형 대시보드 인상. 픽셀 경기장과 톤이 분리됨.

방향(구현 전 사용자 확인 필요):
- 패널을 픽셀 게임 문법으로: 직각/1–2px 스텝 모서리, 단색 면 + 2–3단 명암 테두리, 그림자는 오프셋 하드 섀도 1종.
- 글로우·블러·backdrop-filter·다중 그라데이션 제거. 강조는 팔레트 색 1–2개로만.
- 영문 eyebrow는 화면당 최대 1개, 나머지 한국어 짧은 라벨. 번호 매기기(`01 ·`)는 실제 단계 UI에서만.
- 전역 CSS 변경이므로 AGENTS.md 고위험 규칙 적용: 화면 단위로 나눠 적용, 타이틀 → 경로 → 전투 → 보상 → 시설 전부 모바일 가로/세로 재검증.
- PR #83(V12 UI/UX 기획안)과 겹치는지 먼저 대조한다.

진행: (2026-09-24) 1단계 타이틀 화면만 적용 — `src/duel/title-pixel.css`(전부 `.duel-title` 한정), 영문 eyebrow·오래된 버전 문구 제거. 사용자 미리보기 확인 후 경로 → 전투 → 보상 → 시설 순으로 확장.

### 6. 접근성을 한 번 훑는다

- 9존 그리드를 키보드 방향키로 이동할 수 있는가
- 보상 화면의 4단계가 스크린리더에서 순서대로 읽히는가
- 명암만으로 정보를 전달하는 구간(등급 0)에 색 이외의 단서가 있는가 — **1번과 직접 충돌하므로
  1번을 먼저 끝낸 뒤에 한다**
- `prefers-reduced-motion`을 실제로 존중하는가

발견한 것 중 고칠 수 있는 것만 고치고, 나머지는 STATUS에 남긴다.

### 7. 강화 7종·유물 3종의 상대 가치를 잰다

`scripts/`에 덱 편집과 유물을 실제로 쓰는 리포트를 추가한다. 같은 시드에서 아무것도 없을 때 대비
각 강화/유물 1종만 적용했을 때의 완주율 차이를 낸다. `scout+`(열 정보)가 가장 강할 것으로
예상되며 그게 사실인지 숫자로 확인한다. 자동 정책에 제거·강화·유물 경로가 없으므로
정책 쪽에도 최소한의 경로가 필요하다.

### 9. 라이선스를 정한다

공개 배포 중인 저장소에 이용 조건이 없다. 무엇으로 할지는 사용자 결정이 필요하므로
**직접 고르지 말고** `docs/STATUS.md`에 "사용자 확인 필요"로 올려둔다. 파일을 만들지 않는다.

## 처리 완료

- (2026-09-13) `HANDOFF.md`·`README.md` 최신 2개 절 유지 및 이전 이력 아카이브 — `734a99f`
- (2026-09-13) 현행 import 그래프와 분리된 `src/game` NIGHT RUN / CORE TEST 세대·검사·리포트·전용 스타일 제거 — `4bfb400`
- (2026-09-13) 현행 import 그래프와 분리된 `BaseballSim-deck-5.jsx` 세대·UI 검사·전용 런타임 자산 제거 — `2850d35`
- (2026-09-13) 현행 import 그래프와 분리된 LAST LIGHT 세대·검사·리포트·2.4MB 자산 제거 — `ed1d2dd`
- (2026-09-13) 레퍼토리 확장 순간을 사건·로그·강조로 표시하고 Chrome 1440×1000 화면 확인 — `5966572`
- (2026-09-13) `docs/DUEL.md`를 v8 현행 계약으로 갱신하고 v6 이하를 과거로 표시 — `e36f645`
- (2026-09-13) 읽기 등급 0을 3단계 명암으로 축소 · 죽은 존 은닉 · 판독기 효과 복구 — `f21e06c`
- (2026-09-13) 덱 설계 화면 · 보상 4행동 · 카드 강화 · 성장 시그니처 제거 — `a8ec191`
- (2026-09-13) 투수 레퍼토리 점진 확장 · 읽기 등급 · 유물 3종 — `a8ec191`
- (2026-09-13) 정책 봇이 읽기 등급을 존중 · 등급별 리포트 — `d6dff9a`, `8c7f406`
- (2026-09-13) 모바일 390px 보상 화면 검사 — 수정 불필요. 본문 가로 스크롤 없음(scrollWidth 390),
  칩·변화량·유물·진단 어디에도 잘림 없음. `.reward-cards`가 가로 스크롤 컨테이너라 그 안의
  카드가 뷰포트를 넘는 것은 의도된 동작이다.
