# V12 진행 상황 — 루프의 단일 기준

> **새 세션은 이 파일을 가장 먼저 읽는다.** 한 바퀴 = 체크박스 **한 줄**.
> 도는 방법은 [`V12-LOOP-ENGINEERING.md`](./V12-LOOP-ENGINEERING.md).
> 끝낸 줄에는 **커밋 SHA**를 붙인다.

## 지금 상태

| | |
| --- | --- |
| 현재 단계 | **P2 완료 · 다음 P3(보드)** (D1~D6 사용자 확정 2026-09-24) |
| 브랜치 | `claude/feedback-review-plan-vw24at` |
| PR | [#83](https://github.com/jjsjb88-alt/9-baseball/pull/83) — **P0 완료**: fixture + Before 3뷰포트 + 정의 전용 토큰 + 비차단 검사 골격 + 화면 변화 0 증명. 런타임 UI 변경 0 |
| 추적 이슈 | [#84](https://github.com/jjsjb88-alt/9-baseball/issues/84) |
| 새 세션 프롬프트 | [`V12-NEW-SESSION-PROMPT.md`](./V12-NEW-SESSION-PROMPT.md) |
| 막힌 곳 | D1~D6 사용자 확정 완료. **D7(아트 병행)·D8(범위)은 권고만** — P5 이후에 필요 |

> ### 게이트를 읽는 법 — 반드시 먼저 볼 것
>
> `V12-DECISIONS.md`의 각 항목에는 체크박스가 **두 개**다.
>
> ```
> - [x] 검수자 권고        ← 채워져 있다. 이건 게이트를 열지 않는다
> - [ ] 사용자 확정        ← 비어 있다. 이게 채워져야 열린다
> ```
>
> **`검수자 권고`의 `[x]`를 결정으로 착각하지 마라.** 권고는 설계 제안이고,
> 그 화면을 실제로 만들어도 되는지는 사용자만 정한다.
> 현재 D1~D8 전부 `사용자 확정`이 비어 있으므로 **P1 이후는 열리지 않았다.**

## 다음 한 바퀴

> **P1-1은 아직 시작하지 않는다.** D1 시각 방향의 **사용자 확정**이 필요하다.
> 기존 A/B/C는 사용자에게 모두 거절됐다. D/E/F 중 E/F 계열의 감각은 괜찮다는 반응이 있었지만, 최신 판정은 **“마스터피스가 아닌데?”**다.
> 따라서 E/F를 승인안으로 취급하지 않는다. E의 몰입감 + F의 정보 구조 원리만 참고해 **Golden Master 1안**을 먼저 만든다.
> 상세 인수인계: [V12-D1-GOLDEN-MASTER-HANDOFF.md](./V12-D1-GOLDEN-MASTER-HANDOFF.md)
> 저품질 배우 placeholder는 금지한다. 파일 포맷이 아니라 실루엣·인체·키포즈·명암·축소 판독성·환경 통합감이 품질 게이트다.
> Golden Master를 실제 390×844 PNG로 보여주고 사용자가 명시적으로 승인하기 전까지 D1은 미확정, P1-1은 미착수 상태를 유지한다.
>
> **2026-09-24 Golden Master 1안 제작 완료 — 사용자 확정 대기.** [d1-golden-master.html](./design/v12/d1-golden-master.html) (?v=landscape, ?v=desktop)
> 캡처: [세로](./design/v12/shots/d1-gm-390x844.png) · [가로](./design/v12/shots/d1-gm-844x390.png) · [PC](./design/v12/shots/d1-gm-1440x900.png). 기록: [gm/README.md](./design/v12/gm/README.md) (루프 12회, 독립 리뷰 3회).
> 한 픽셀 격자 + DUGOUT 원화 재샘플링 + Galmuri(OFL) 픽셀 글꼴. 칸·연결·효율은 gm-state.mjs가 엔진으로 계산(C1~C6). 런타임 변경 0.
> CI V12 P0 Verify run `35921648101` 성공. **남은 한계는 원화**(배우-경기장 밀도, 정면 투수)이며 코드만으로는 판정이 크게 바뀌지 않는다.
> **D1 사용자 확정은 비워 둔다.** 사용자가 이 캡처를 보고 승인하면 체크하고 P1-1을 연다.

---

## P0 · 기준선 (결정 불필요 — 지금 가능)

- [x] **P0-1** 기준 SHA 고정 + 전투 대표 상태 fixture(seed·저장 JSON) + 수집 스크립트 — `fa42bd8f`
- [x] **P0-2** 3뷰포트 Before 캡처 세트를 `docs/design/v12/shots/`에 커밋 — `ef86dccc`
- [x] **P0-3** `src/duel/tokens.css` 신설 (정의만, 적용 0) — `e26ce769`
- [x] **P0-4** `tests/design-system.test.js` 골격 — 현재값 기록만, 감축 임계값 없음 — `4470fd42`
- [x] **P0-5** 화면 변화 0 증명: 캡처 비교 + `pnpm test` + `pnpm build` + `zone-report` 불변 — `6c44a073`

> **P0-1 검증 증거:** 기준 SHA `a1cbf625fa558bb463999fee47998868d155fcb4`, seed `12012026`,
> fixture `docs/design/v12/fixtures/p0-1-combat.json`, collector `scripts/v12-baseline.mjs`.
> Actions run `35798638543`: 53파일 / 414테스트 통과, production build 통과,
> frozen baseline 대비 `zone-report.js 30` exact diff 0, V12 static/engine audit 통과, fixture 재생성 diff 0.
> 390×844 / 844×390 / 1440×900 캡처는 오류 0으로 생성했고 직접 읽었다.
> 세로는 `scrollHeight 894`로 50px 스크롤, 가로·PC는 문서 오버플로가 없었다.
> 이 캡처는 P0-1 검증 증거이며 **저장소에 Before PNG를 커밋하는 일은 P0-2**다.

> **P0-2 검증 증거:** `docs/design/v12/shots/p0-1-390x844.png`,
> `p0-1-844x390.png`, `p0-1-1440x900.png`을 실제 viewport 크기로 커밋했다.
> 실패 검사 `eb92727a`는 세 파일 부재만으로 414 pass / 1 fail이었고,
> 구현 후 read-only 검증 run `35812887851`에서 **415/415 테스트**, production build,
> frozen baseline 대비 `zone-report.js 30` exact diff 0, V12 audit, fixture 재현성, 3뷰포트 재캡처가 모두 통과했다.
> 세 이미지를 직접 읽었고 실제 타자 대신 왼쪽 임시 stand-in이 남아 있는 현재 문제를 Before 사실로 기록했다.

> **P0-3 검증 증거:** 실패 검사 `be536a47`는 `tokens.css` 부재만으로 기존 53파일은 통과하고
> 신규 토큰 검사 2건만 ENOENT로 실패했다. 구현 `e26ce769`은 `src/duel/tokens.css`에
> 타입·색·간격·반경·조작영역·z-index·UI 모션 값을 **정의만** 추가했고 runtime import는 0이다.
> Actions run `35814217956`: **54파일 / 418테스트 통과**, production build,
> frozen baseline 대비 `zone-report.js 30` exact diff 0, V12 audit, fixture 재현성, 3뷰포트 재캡처 모두 통과.
> audit 수치도 `!important 2266`, <11px 433, <12px 493으로 불변이다.
> 390×844 / 844×390 / 1440×900 이미지를 직접 읽었고 기존 타자 stand-in을 포함해 화면 변화가 없음을 확인했다.

> **P0-4 검증 증거:** red-first `c1ab5489`는 기존 **418/418**을 유지한 채 새 scaffold marker 1건만 의도적으로 실패했다.
> 구현 `4470fd42`는 `tests/design-system.test.js`에서 공용 audit를 호출해 P0 현재값
> (CSS 22파일 / 5332개행 / !important 2266·1024줄 / 색 2513·distinct 2107 / <11px 433 / <12px 493)을 역사 기록으로 남긴다.
> **현재값과 기록값의 equality·최대/최소·감축 목표 비교는 하지 않는다.** run `35820148038`에서
> **55파일 / 419테스트**, production build, frozen-baseline `zone-report 30` exact diff 0,
> V12 audit, fixture 재현성, 3뷰포트 재캡처가 모두 통과했다. 이미지를 직접 읽어 화면 변화 0을 확인했다.

> **P0-5 검증 증거:** 첫 red `f75ff43f`에서 `node ... | tee`가 Node 실패를 숨기는 CI 파이프 버그를 발견했고,
> `408582d1`에서 `pipefail`을 켜 비교 스크립트 부재가 마지막 게이트에서 실제로 실패함을 확인했다.
> `73aa8f49`의 첫 고정 Before 비교는 PC의 sparse Canvas/VFX 차이 연속 덩어리 166px만 예비 128px 한도를 넘었고
> 다른 픽셀 지표는 모두 통과했다. 이를 근거로 bbox 보고를 추가하고 캡처 잡음 한도를 192px로 보정한 `6c44a073`에서
> Actions run `35822610676`이 **55파일 / 419테스트**, production build, frozen-baseline `zone-report 30` exact diff 0,
> V12 audit, fixture 재현성, 3뷰포트 재캡처·픽셀 비교를 모두 통과했다.
> 고정 Before 대비 변경 픽셀 비율은 390×844 **0.0453%**, 844×390 **0.1516%**, 1440×900 **0.1478%**였고,
> 최대 연속 덩어리는 각각 10 / 60 / 79px였다. 세 이미지를 직접 읽어 레이아웃·스타일 변화가 없고
> 기존 타자 stand-in 문제도 그대로임을 확인했다. 이는 PNG 바이트 동일이 아니라 **측정된 Canvas/VFX 캡처 잡음 안의 화면 변화 0** 증거다.

> P0에서 `!important` 총량 감축·일괄 `@layer` 래핑을 **하지 않는다**(C7).

## P1 · 대표 전투 화면 가독성 — D1 필요

- [x] **P1-1** D1 확정 반영: 전투 화면 골격 배치(HP/보드/손패/실행 버튼) — P1-1a/b/c
  - [x] **P1-1a** 세로 390×844 · 설계 상태(스윙/준비) 골격 — `1a50240`
    영향도: UI 레이아웃 **있음**(세로 미디어쿼리 + `.duel-combat.v12-design`에만 한정) · 입력 없음 · 게임 로직/밸런스 없음(zone-report 30 불변) · 저장 없음 · 모바일 세로 **있음** · 스크롤 **있음**(설계 상태 1460px → 844px, 실행 칸은 스윙 준비 시 하단 고정) · 기존 플로우: 허브·지켜보기·판정·지도·보상 **없음**(클래스가 붙지 않음, 허브 894px 그대로) · 테스트 **있음**(신규 `v12-p1-battle-portrait.test.js`) · 빌드 **있음**(CSS 1개 추가).
    배치: 전광판 줄 = 타자·카운트 | 투수 HP(중복이던 PITCHER HP 칸은 숨김) → 경기장 안에 투수 의도(왼쪽 위)·투수(먼 마운드)·타자(왼쪽 아래)·9ZONE(오른쪽 아래, 판 윗변에 READ 태그) → READ → 돌아가기+지켜보기 한 줄 → 손패 → 실행 칸.
    남김: 9존 칸 48px, 배치 방법 안내, HP 배율·카드 수별 배율, 결과 이벤트 줄. 줄임: 장식 머리말(SWING, READ → BET), 하단과 중복된 안내 2줄.
    검증: 브라우저 390×844 fixture 스윙 상태 문서 844px·카드 선택 후 실행 버튼 뷰포트 안(793~831px) 직접 확인, 56파일/423테스트, build, zone-report 불변. 미검증: 가로·PC(P1-1b/c), 준비 모드(fixture에 준비 카드 없음), 200% 확대.
  - [x] **P1-1b** 가로 844×390 · 설계 상태 골격 — `9fca4de`
    영향도: UI **있음**(`(orientation:landscape) and (max-height:500px)` + `.duel-combat.v12-design`만) · 입력 없음 · 로직/밸런스 없음(zone-report 불변) · 저장 없음 · 가로 **있음** · 스크롤 없음(문서 844×390 유지) · 플로우 없음 · 테스트 **있음**(`v12-p1-battle-landscape.test.js`) · 빌드 **있음**(CSS 1개).
    배치: 왼쪽 470px 경기장(전광판·HP·READ·투수·타자, 배우 크기 고정) · 오른쪽 위 9ZONE(204px, 칸 50px) · 그 옆 열: 한 구 지켜보기(44px) · 실행 칸(카드 선택 시 실행 버튼 132×56 위, DETAIL 아래) · 오른쪽 아래 손패.
    P0 화면 게이트: 수집기가 가로에서 스윙 상태로 들어가므로 `baseline.json` `intentionalChanges`에 844×390을 사유와 함께 올려 판정에서 제외(측정·보고는 유지). 세로 허브·PC는 기존 기준 그대로. 이 게이트는 런타임 무변경 커밋(229a3ac·58b29bc·dc37c69)에서도 실패한 적이 있는 경계선 노이즈 검사이며, P1-1a CI 실패(35924074852)도 로컬 전후 비교에서 노이즈 범위였다.
    검증: 브라우저 844×390 fixture 스윙 상태·카드 선택 후 실행 버튼 뷰포트 안 확인, 57파일/426테스트, build. 미검증: PC(P1-1c), 200% 확대.
  - [x] **P1-1c** PC 1440×900 · 설계 상태 골격 — `551f925`
    영향도: UI **있음**(`(min-width:1080px) and (min-height:650px)` + `.duel-combat.v12-design`만) · 입력/로직/저장/플로우 없음 · 테스트 **있음**(`v12-p1-battle-desktop.test.js`) · 빌드 **있음**.
    배치: 타자 왼쪽 전경 · 투수 가운데 마운드(170px) · 투수 의도 그 왼쪽 · 9ZONE 오른쪽(380px) · 손패 아래 가운데(590px) · 지켜보기(176×56)·실행(카드 선택 시 182×100) 오른쪽 아래 · BATTLE READOUT은 왼쪽 아래 빈자리.
    P0 화면 게이트: 로컬 Edge 재캡처로 세로 허브는 P1 전후 차이가 노이즈와 같음을 확인(변화율 0~0.00044, 전-전 노이즈 0~0.00184). PC는 P1 이전 커밋끼리도 연결 성분 1,291px로 한도(192px)를 넘는 흔들림이 있어 P1-1b CI 실패(35924994082)의 유력 원인이며, P1-1c로 의도 변경되므로 `intentionalChanges`에 사유와 함께 추가. 세로 허브는 원래 한도로 계속 검사.
    검증: 브라우저 1440×900 스윙·카드 선택 후 실행 버튼 확인, 58파일/429테스트, build. **P1-1 세 뷰포트 골격 완료.** 남은 P1: P1-2 타이포, P1-3 스크롤·200% 확대.
- [x] **P1-2** 타이포 스케일 적용 · 11px 미만 433건 / 12px 미만 493건 처리(확대 또는 삭제) — P1-2a/b: 전투 화면 렌더 기준 12px 미만 0(P4 결과 패널·P2 카드 제외). 정적 집계는 433/493 그대로(덮어쓰기, 기존 선언 정리는 C7 후속)
  - [x] **P1-2a** 전투 설계 상태 HUD 글자 12px 하한 — `67896f3`
    렌더 감사(Edge, fixture 스윙 상태, 카드 내부·경기장 그림 글자 제외): 세로 26종 → 0, 가로·PC 스크립트 주입 라벨 포함 → 0. 문서 크기 세 뷰포트 모두 뷰포트와 동일(390×844 · 844×390 · 1440×900).
    처리: 리본·전광판·HP·투수 의도·이름표·베이스 표시·READ·9존 칸(띠 14px/숙련 12px)·배율 칩·손패 머리말·지켜보기·실행 칸 안내·푸터를 12px 이상으로. 9존 칸의 위치 글자(IN·HIGH)는 보드 테두리 범례(◀ 몸쪽 · 바깥쪽 ▶)로 옮기고 칸 aria-label("몸쪽 높음")에는 그대로 남김. 중복이던 FULL/-20% 태그·배치 방법 안내·푸터 표어는 설계 상태에서 뒤로 물림.
    **정적 집계는 433 / 493 그대로**: 기존 선언을 덮어썼을 뿐 지우지 않았다. 수치 감소는 컴포넌트별로 기존 선언을 정리하는 후속 슬라이스(C7)에서. 카드 내부 글자(규칙문 7px·도착 확률 6.5px 등)는 P2(카드, D3/D4)로 넘김. 가로 HP 카드는 기존 층이 숫자를 바 위에 겹쳐 둠 → P1-3에서 확인.
  - [x] **P1-2b** 모든 전투 상태(허브·지켜보기·결과·다음 타자)의 HUD 12px 하한 — `7018e62`
    렌더 감사(허브/지켜보기/결과 × 세 뷰포트): 세로 허브 약 30종(전광판 6px·의도 7px·베이스 6~7px·READ 7~9px·YOUR CALL 7~9px 등) → 0. `v12-battle-type-hud.css`(전투 루트 한정)가 HUD·허브 선택지·HP 결과 요약·스크립트 라벨을 올리고, 설계 상태가 아닌 9존 칸도 위치 글자를 테두리 범례로 옮김(aria-label 유지).
    남김(범위 밖): 투구 결과·해설 패널(`.pa-result`, `.decision-debrief`, 5~10px) → P4, 카드 내부 → P2. 세로 허브 문서 894 → 929px(글자 확대분). 헤드리스 캡처의 결과 화면에서 "저장 실패" 배너가 떴으나 앱 내 브라우저에서 같은 조작으로는 재현되지 않음(헤드리스 저장소 문제로 판단, 코드 변경과 무관).
    **P0 화면 게이트 은퇴(보고만):** P1-2b로 세로 허브까지 바뀌어 P0 캡처 3장이 모두 P1의 의도 변경이 됨(CI 35928636835 실패가 그 결과). `intentionalChanges`에 세 장 모두 사유와 함께 기록 — 측정·보고는 계속하지만 판정하지 않는다. 이후 전투 화면 증거는 P1 계약 테스트 · 실제 입력 레이아웃 검사 · 렌더 글자 감사가 맡는다.
- [x] **P1-3** 스크롤 · 844×390 가로 · 200% 확대 확인 — P1-3a/b (375×667은 스크롤 필요, 실행 버튼 고정)
  - [x] **P1-3a** 실제 입력 검사 + 가로 층 비율화(200% 확대) — `35c9ee3`
    방법: Edge(playwright-core) fixture 스윙 상태에서 카드 탭 → 9존 칸 탭으로 스윙을 준비한 뒤, 9존·손패·지켜보기가 뷰포트 안인지, 실행 버튼이 뷰포트 안이며 `elementFromPoint`로 실제로 눌리는지 확인.
    발견·수정: (1) 가로 층이 844px 고정 좌표라 720×450(1440×900의 200% 확대)에서 조작 열이 잘림 → 경기장 56%·9존 24%·조작 열 17% 비율로 재작성, 오른쪽을 하나의 dock(조작 열 위·손패 아래)으로 만들고 dock 빈 영역은 포인터 통과. (2) 기존 층의 dock `max-height` 때문에 손패가 위로 뜨고 실행 버튼이 가려져 눌리지 않았음 → 해제. (3) 스윙 준비 시 BATTLE READOUT이 9존을 덮음 → 조작 열의 실행 칸 아래로 이동(넘치면 그 안에서 스크롤).
    결과: 844×390 · 720×450 · 390×844 · 1440×900 모두 9존/손패/지켜보기 뷰포트 안, 실행 버튼 보임·눌림, 문서=뷰포트. 375×667(작은 휴대폰)은 문서 861px로 스크롤 필요, 실행 버튼은 하단 고정·눌림(한계로 기록).
    남음: 가로 HP 카드 숫자-바 겹침, 준비 모드(fixture에 준비 카드 없음), 지도·보상·시설 화면 회귀는 이 층이 적용되지 않아 무변경.
  - [x] **P1-3b** 가림·겹침 정리 + After 캡처 — `b1ce827`
    가로: HP 숫자를 바 아래로(기존 층이 바 위에 띄움), 전광판·HP 카드 뒤에 숨어 있던 투수 의도를 전광판 아래로, 의도와 겹치던 장식 "AT BAT" 표지는 전광판과 중복이라 숨김.
    세로: 12px 하한 후 경기장이 줄어 스윙 준비 상태에서 9존이 투수를 덮음 → 9존 안의 HP 배율·카드 수 배율 블록을 9존 왼쪽(의도 아래)으로 옮기고 9존은 칸 격자만, 경기장 최소 290px·투수 88px로 먼 마운드에 온전히 보임. 문서 857px(13px 스크롤), 실행 버튼은 하단 고정·눌림.
    준비 모드: 손패에 준비 카드를 넣은 브라우저 상태로 확인 — `v12-design` 적용·한 화면. 실행 칸 문구가 준비 모드에서도 "카드를 9존에 놓으세요"인 것은 기존 문구로 P2 검토 항목.
    After 캡처(Edge 로컬, fixture): `docs/design/v12/shots/p1-{390x844,844x390,1440x900}-{swing,armed}.png`. Before는 P0 `p0-1-*.png`(CI Linux)라 폰트·렌더러가 다르다.

## P2 · 카드 — D1·D3·D5 필요

- [x] **P2-1** 카드 이름·상세 시트(길게 누르기) — `3e94513`
    영향도: UI(새 모달 z70, 실행 칸에 버튼 1개) 있음 · 입력(길게 누르기) 있음 → window capture 단계, 8px 이동·pointercancel·scroll이면 취소(스택 드래그 9px보다 먼저 포기), 길게 누른 뒤의 click 1회만 삼킴 · 게임 로직/저장/빌드 영향 없음(zone-report 불변) · 모바일/스크롤: 세로 바텀시트·가로/PC 가운데, 시트 내부 스크롤 · 기존 흐름: 카드 aria-label·이름 불변(D3/D4), 카드에 `data-card-kind/plus/problem`만 추가.
    구현: `CardDetailSheet.jsx` + `card-detail.css`. 들어가는 길 셋 — 선택 카드의 **ⓘ 설명** 버튼(실행 칸, 44px), 손패 카드에 포커스 후 **i** 키, **길게 누르기/우클릭**(보조). 시트: 이름·종류·역할·축·원문 규칙(`cardText`)·효과/조건 태그·강화 전후·사용 불가 이유·설명문. Escape/닫기/배경 탭으로 닫고 포커스는 연 요소로 복귀, Tab은 시트 안에서 순환, 배경 입력 차단.
    검증: `tests/v12-p2-card-detail.test.jsx` 13개(16종 이름·원문 유지, 강화 문구, BASIC, 모달 속성·Escape·Tab·배경, 길게 누르기 480ms·빠른 탭·드래그·취소·i 키·우클릭, 전투 통합·포커스 복귀, CSS 12px/44px/범위). 브라우저 실제 입력(390×844·375×667·844×390·720×450·1440×900): 길게 누르기 → 시트, 스윙 무장 안 됨, Escape 뒤 스윙 유지, 카드→칸 뒤 ⓘ 설명·실행 버튼 모두 뷰포트 안·실제로 눌림, 포커스 복귀.
    발견·수정: 세로에서 ⓘ 버튼이 실행 버튼을 115px로 좁힘 → 자기 폭만 쓰고 실행이 나머지. 가로에서 BATTLE READOUT·DETAIL 토글과 겹침 → ⓘ 설명과 DETAIL을 한 줄로, readout 180px로.
    캡처: `docs/design/v12/shots/p2-1-{390x844,844x390,1440x900}-{sheet,armed}.png`.
- [x] **P2-2** `coverageAt` 기반 정확한 커버 미리보기 (C1) — `092459f`
    영향도: UI(9존 칸 클래스만 토글) 있음 · 입력: 포인터/포커스를 **passive로 듣기만** 함, 스택 직접 배치 흐름 불변 · 게임 로직/저장 영향 없음(zone-report 해시 동일) · 모바일: 가로/PC 드래그 중 보드가 움직이던 기존 버그 발견·수정(아래) · 빌드 영향 없음.
    구현: App이 스윙 카드마다 `coverageAt(s,id,z)`를 9존 전부 계산해 `data-cover-map`에 씀(`coverMapOf`). `cover-preview.js`는 탭으로 든 카드(`board-card-armed`)나 끄는 카드(`board-drag-source`)가 있을 때, 포인터 아래 칸·드롭 대상 칸·키보드 포커스 칸 기준으로 **그 목록의 칸만** 표시: 메인(첫 카드) 실선 금색, 지원 점선 청록, 반투명 채움(D2). shape 이름으로 추정하지 않음 → 모서리 십자 3칸, `expanded`·기다림 성장 반영.
    발견·수정(P1 회귀): 가로(844×390·720×450)와 PC(1440×900)에서 카드를 들면 기존 층이 9존을 `translateX(-50%) scale(1.035)`로 옮기고 칸을 키워 **손가락 아래 목표 칸이 사라져 드래그 배치가 실패**했다(옛 가운데 정렬 보드 전제). V12 층에서 이동·확대를 고정.
    검증: `tests/v12-p2-cover-preview.test.jsx` 8개(9존 전부 coverageAt 일치, expanded 반영, 준비 카드 없음, 손패 속성, 탭-호버/드래그/키보드/해제, 메인·지원 구분, CSS 범위). 브라우저 실제 입력 4크기: 밀어치기 들고 0번 칸 호버 → [0,3,6] 메인, 탭 배치 후 해제·실제 커버 [0,3,6], 정타 노림을 8번 칸으로 드래그 → [8] 지원, 놓으면 2장 배치(HP 80%).
    캡처: `docs/design/v12/shots/p2-2-{390x844,844x390,1440x900}-{hover,drag}.png`.
- [x] **P2-3** 준비 카드 구별 — `0ba3cdd`
    영향도: UI(설계 상태 손패의 준비 카드 외형) 있음 · 입력: 버튼 크기·위치 불변(원은 `::before`로만 그림, 모양 맞춤 hit area 없음 — D5) · 게임 로직/저장 영향 없음(zone-report 해시 동일) · 모바일 3뷰포트 확인 · 기존 흐름: 이름·aria-label 불변.
    구현: `v12-card-prepare.css` — 스윙 카드는 홈플레이트 카드, 준비 카드는 **원형 토큰**(뼈색 원·청록 테두리, 선택 시 금색). 원 안에 아이콘·**이름**·**"준비 N회 남음"**(`card-uses`, 엔진 `preparations`에서). 쓸 수 없는 카드는 회색 점선 원에 **사용 불가 이유**(엔진 problem). 규칙 원문은 P2-1 상세 시트. 준비 모드 실행 칸(카드 선택 전) 문구를 "카드를 9존에 놓으세요" → **"준비 카드를 고르세요 · 남은 준비 N회"**로(P1-3b에서 넘긴 항목). 실행 버튼은 "이름 · 준비 1회".
    발견·수정: (1) 가로에서 스윙→준비 전환 시 손패가 끝에 스크롤된 채라 준비 카드가 화면 밖 → 모드 전환 때 선택 카드(없으면 맨 앞)를 보이게. (2) PC 손패가 가운데 정렬로 넘쳐 **왼쪽 카드가 스크롤로도 닿지 않던** P1 버그 → `justify-content:safe center`.
    검증: `tests/v12-p2-prepare-cards.test.jsx` 8개(이름 유지·남은 횟수·불가 이유·준비 모드 문구·실행 버튼·스윙 모드 문구 유지·CSS 범위·hit area 불변·12px). 브라우저 390×844·844×390·1440×900·720×450: 준비 모드 진입·카드 선택·실행 버튼 문구, 첫 카드가 손패 안에서 시작.
    한계: 가로(844×390) 토큰 지름 78px라 불가 이유는 2줄로 잘림(전체는 상세 시트). 준비 모드에서 9존이 숨는 기존 동작으로 가로 오른쪽 위가 비어 있음 → P3 보드 작업에서 검토.
    캡처: `docs/design/v12/shots/p2-3-{390x844,844x390,1440x900}-{prepare,prepare-armed}.png`.
- [x] **P2-4** BASIC·번트·강화·지원 역할 전수 확인 — `fa09cf7`
    방법: fixture 손패에 희생 번트·밀어치기+·커트 스윙(십자)을 넣고 4크기(390×844·844×390·1440×900·720×450)에서 실제 입력으로 역할별 확인. 콘솔 오류 0.
    결과: BASIC — 1칸 커버, "기본 스윙". 번트 — 미리보기·실제 커버 9칸, "희생 번트 · 단독". 강화 — 손패 "밀어치기+", aria "밀어치기 강화", 상세 "강화됨 · 파워 +18". 지원 — 모서리(8번) 십자 미리보기 **[5,7,8] 3칸**(shape 5칸 아님, C1), 합집합 [2,5,7,8], "2장 배치 스윙 · HP 80%".
    발견·수정: (1) 강화 카드 실행 버튼에서 "+"가 빠짐("밀어치기 · 단독") → "밀어치기+ · 단독". (2) 번트 메인 위에 카드를 놓으면 엔진 규칙상 겹치지 않고 **메인이 교체**되는데 미리보기는 지원(점선)으로 보였음 → BASIC·번트·번트 메인 위 카드는 메인(실선)으로 미리보기. (3) 상세 시트에 겹치기 규칙 한 줄(번트: 엔진 문구 그대로 "겹치기 불가", BASIC: "메인 전용", 스윙 카드: "겹치기 가능").
    검증: 테스트 5개 추가(교체 미리보기 2, 상세 겹치기 규칙 2, 강화 실행 버튼 1). 캡처 `docs/design/v12/shots/p2-4-{390x844,844x390,1440x900}-{bunt,support}.png`.

## P3 · 보드 — D1·D2·D4 필요

- [x] **P3-1** 커버 셀 표시 (C1) — `e753918`
    영향도: UI(설계 상태 9존 칸 외형) 있음 · 입력 영향 없음(클래스·숨은 설명만) · 게임 로직/저장 영향 없음(zone-report 해시 동일, 엔진 값 표시만) · 접근성: 칸 이름(aria-label) 유지, 덮인 칸은 `aria-describedby`에 "메인 커버/지원 커버" 추가 · 결과 공개(revealed) 후에는 적용 안 함(P4).
    구현: ZoneBoard가 엔진 미리보기 `primaryCoverage`·`supportCoverages`(모두 coverageAt)로 칸에 `cover-main`/`cover-support`를 붙임, 단독 카드는 전부 메인. `v12-board-cover.css`: 메인 = 금색 실선 테두리 + 옅은 금색, 지원 = 칸 안쪽 청록 점선 고리 + 옅은 청록, 겹치면 둘 다(D2 반투명, 불투명 카드 없음). 배치 미리보기(P2-2)·충돌 섬광은 그 위.
    검증: `tests/v12-p3-board-cover.test.jsx` 5개. 브라우저 3뷰포트: 밀어치기+ 3번 칸 + 커트 스윙 9번 칸 → 메인 [2,5,8], 지원 [5,7,8], 겹침 [5,8].
    캡처: `docs/design/v12/shots/p3-1-{390x844,844x390,1440x900}-stack.png`.
- [x] **P3-2** 순번 · 순서 링크 (C2) — `2e00e25`
    영향도: UI(9존 위 SVG 한 장, pointer-events:none) 있음 · 입력 영향 없음(포인터를 받지 않음, 칸 조작 그대로) · 게임 로직/저장 영향 없음(zone-report 해시 동일) · 접근성: SVG는 aria-hidden, 같은 내용을 숨은 status 문장으로("순서 연결 · ①→② 끊김") · 결과 공개 후 숨김(P4).
    구현: `ZoneLinks.jsx` — `stackPlan.links` 한 항목당 표시 하나. 연결 = 청록 실선, 끊김 = 갈색 점선 + 가운데 틈(색만이 아니라 선 모양으로 구분), 같은 존 = 고리. 연결 여부는 엔진(`v11StackZonesConnect`) 값 그대로, 모서리 접촉·커버 겹침으로 계산하지 않음. 끝점은 칸 위 순번 토큰(①②) — 없으면 칸 중심. 실제 렌더된 칸·토큰을 재서 세로·가로·PC 모두 맞춤(ResizeObserver + 토큰 추가 감지).
    검증: `tests/v12-p3-board-links.test.jsx` 7개(요약 문장, 선/틈/고리 좌표, 엔진 대각 연결·먼 존 끊김, SVG 표시 수·aria-hidden, 링크 없음, 전투 통합, CSS). 브라우저: 3번+9번 칸 → 끊김 점선(3뷰포트), 3번+5번 칸 → 실선, HP 87%·CONNECT 1/1.
    캡처: `docs/design/v12/shots/p3-2-{390x844,844x390,1440x900}-break.png`, `p3-2-390x844-connect.png`.
- [x] **P3-3** 효율 표시 — 배율과 HP 분리 (C3) — `SHA_P33`
    영향도: 문구만(스윙 보드 안내·실행 버튼) · 입력/게임 로직/저장 영향 없음(zone-report 해시 동일) · 기존 테스트의 "HP ×0.80" 문자열 계약을 "×0.80"으로 의도 갱신(C3 근거).
    발견·수정: (1) **C1 위반** — 보드 안내가 "2장 배치 · 커버 2칸"처럼 **카드 수를 커버 칸 수로** 표시(실제 4칸) → 엔진이 칠한 커버 칸 수. (2) "HP POWER / HP ×0.80", 실행 버튼 "HP 80%"가 HP 값처럼 읽힘 → "피해 효율 ×0.80", "2장 배치 스윙 · 피해 효율 80%", "밀어치기 · 단독 스윙 · 피해 효율 100%". 현재 HP(전광판 72/72)와 배율이 다른 단위로 분리됨. 결과 요약의 "투수 HP 피해 -N"은 실제 피해라 그대로.
    검증: `tests/v12-p3-efficiency.test.js` 3개 + 기존 직접 배치 테스트 갱신. 브라우저 4크기: "2장 배치 · 커버 4칸"(합집합 [2,5,7,8]), 실행 버튼 넘침 없음.
    캡처: `docs/design/v12/shots/p3-3-{390x844,844x390,1440x900}-efficiency.png`.
- [ ] **P3-4** 동일 존 선택 · 회수 · 재정렬 · 확정 1회 — `_`
- [ ] **P3-5** 입력: native 스크롤 우선 · 전용 핸들 · 탭/키보드 대안 · cancel — `_`

## P4 · 판정 연출 — D1·D6 필요

- [ ] **P4-1** 공개된 실제 결과 기반 인과 연출 — `_`
- [ ] **P4-2** 모든 결과 종류 + 다음 공/타자 + reduced-motion — `_`

## P5 · 지도

- [ ] **P5-1** 노드 표현 + 선택 상세 — `_`
- [ ] **P5-2** 위험·보상 비교, 세로 스크롤 — `_`

## P6 · 보상 · 시설 · 덱

- [ ] **P6-1** 정확한 효과·비용·강화 비교 — `_`
- [ ] **P6-2** 근거 없는 히트맵 제외 확인 — `_`

## P7 · 안내

- [ ] **P7-1** 각 단계에서 검증된 것만 도움말·첫 사용 안내로 정리 — `_`
- [ ] **P7-2** 신규/기존 프로필 · 스킵 · 재생 · 저장 실패 — `_`

## P8 · 종료 화면 · 도감

- [ ] **P8-1** 실제 기록 기반 종료 화면 — `_`
- [ ] **P8-2** 도감 — 전투에서 뺀 문장의 집 — `_`

---

## 마무리

- [ ] `V12-MASTERPIECE.md` §12 완료 정의 전 항목
- [ ] 사람 검증 3문답 통과
- [ ] `HANDOFF.md` 최신화
- [ ] **사용자 배포 승인** (에이전트가 단독으로 하지 않는다)

---

## 막힌 곳 · 기록

| 날짜 | 무엇이 막혔나 | 필요한 결정 | 상태 |
| --- | --- | --- | --- |
| 2026-09-22 | P1 이후 전부 | D1 시각 방향 | **2026-09-24 사용자 확정**(D1 Golden Master) |
| 2026-09-22 | P3 커버 표현 | D2 커버 표현 | **2026-09-24 사용자 확정**(권고대로) — D3·D4·D5·D6도 함께 확정 |
