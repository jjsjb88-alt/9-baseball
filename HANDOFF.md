# 9ZONE SHOWDOWN — Codex 인수인계

## 1. 제품 가설

이 게임의 핵심 감정은 “확률이 높은 곳을 눌렀다”가 아니라 **“내가 저 투수를 읽었다”**다. 따라서 기능 수나 카드 수보다 아래 한 장면을 먼저 검증한다.

> 공개 확률은 낮지만 앞선 투구에서 발견한 습관을 믿고 특정 존에 POWER를 건다. 실제 공이 그 존에 들어오고 `DEEP READ`가 뜬 뒤 장타가 날 수 있다. 틀리면 큰 대가를 치른다.

봇 시뮬레이션은 지배전략을 찾는 도구이지 재미의 증명이 아니다. 최종 판단은 인간 플레이테스트로 한다.

## 2. 코어 루프: READ → BET → REVEAL → IMPACT

- **READ**: PUBLIC 확률, 카운트, 앞선 투구 기록, 투수 성향으로 위치를 예측한다.
- **BET**: 존 1장, 인접 존 2장, 존+수식어 또는 카드가 없는 존의 BASIC SWING을 선택한다.
- **REVEAL**: 이미 TRUE INTENT에서 샘플링된 실제 존을 공개한다. `READ`, `DEEP READ`, `COVERED`, `NEAR READ`, `MISREAD`, `CHASE`를 판정한다.
- **IMPACT**: READ 결과·숙련·수식어를 CQ에 넣어 접촉을 먼저 판정한다. 접촉한 뒤 PQ로 단타/장타/홈런을 판정한다.

READ는 HIT와 동의어가 아니다. 완벽히 읽고도 헛스윙할 수 있고, 잘못 읽고도 운 좋은 안타가 나올 수 있어야 한다.

## 3. 시작 4숙련존과 BASIC SWING

시작 숙련존은 `[중상(1), 좌중(3), 정중(4), 중하(7)]` 네 곳이다. 이전의 9존 시작 루프를 되살리지 않는다.

손패에 해당 존 카드가 없어도 스트라이크존 격자를 눌러 BASIC SWING을 할 수 있다. BASIC은 카드를 소모하지 않지만 비숙련 CQ/PQ 페널티를 받는다. 목적은 “카드가 없어서 아무것도 못 함”을 없애되 덱 성장의 의미는 유지하는 것이다.

## 4. CQ/PQ 판정

`resolveShowdownContact()`가 실제 플레이와 자동시뮬의 공용 엔진이다.

- **CQ(Contact Quality)**: READ 정확도, 숙련 여부, CONTACT/POWER/CUT, 2존 커버, 투수 구위, 분산으로 접촉 여부를 결정한다.
- **PQ(Power Quality)**: 접촉 이후 READ, 숙련, POWER, 2존 커버, 분산으로 타구 등급을 결정한다.
- **POWER**: CQ를 낮추고 PQ를 크게 올린다. 정확히 읽지 못하면 강하지 않다.
- **CONTACT**: CQ를 높이고 PQ를 낮춘다.
- **CUT**: 일부 실패를 파울로 바꾸어 생존 시간을 산다.
- **2존 COVERED**: 접촉 보험이지만 PERFECT/DEEP READ가 아니며 장타가 약하다.

구형 `resolveContact()`는 투수 역할 등 아직 남은 레거시 경로를 위한 호환 코드다. 타자 코어와 동료 타자 자동시뮬은 반드시 `resolveShowdownContact()`를 사용한다.

## 5. PUBLIC MODEL vs TRUE INTENT

- **PUBLIC**: 플레이어에게 보여 주는 분포. 일반 제구 모델과 관찰 노이즈 기반이다.
- **TRUE INTENT**: AI 단계, 카운트, 숨은 습관, 과거 플레이어 행동을 반영한 실제 샘플링 분포다.
- TRUE 수치는 UI에 직접 노출하지 않는다. 투구 후에도 숫자를 공개하지 않고 `DEEP READ` 피드백만 준다.
- 낮은 PUBLIC 확률을 우연히 맞힌 것만으로 DEEP READ를 주지 않는다. 선택 존의 TRUE가 PUBLIC보다 의미 있게 높아야 한다.

## 6. ROOKIE / ADAPTER / FOX

- **ROOKIE**: 읽을 수 있는 강한 Tell. 현재 구현은 2스트라이크에서 중하 승부구를 강하게 선호한다.
- **ADAPTER**: Tell을 약화하고 최근 네 번의 플레이어 노림 존을 피한다.
- **FOX**: Tell을 더 약화하고 최근 여덟 번의 노림을 더 강하게 역이용한다.

AI는 `playerAimHistoryRef`의 완료된 과거 선택만 본다. `selectedIdx`, `comboRef`의 현재 선택을 투구 결정에 사용하면 안 된다.

## 7. TURN GUARD

유저 타자는 4번 타순이다. 시작 직후 AI 투구를 강제 호출하지 않는다. 1~3번 동료 타석을 자동 처리하고 유저 슬롯 진입 effect가 첫 투구를 시작한다.

자동시뮬 함수 첫 줄에서 현재 타순 ref를 다시 검사한다. 이미 예약된 타이머가 늦게 실행되더라도 유저 슬롯이면 즉시 종료한다. 자동 타석 종료 후 다음 타자가 유저라면 다음 자동 타이머를 예약하지 않는다.

## 8. 자동시뮬 일치 원칙

과거 가장 반복된 사고는 “밸런스 시뮬레이터의 규칙과 실제 게임의 규칙이 다름”이었다. 동료 타자 자동시뮬도 공용 `resolveShowdownContact()`를 호출한다. 앞으로 정책봇을 추가할 때도 이 함수를 import해서 사용한다. 판정식을 복사해 새로 쓰지 않는다.

## 9. 테스트 체크리스트

### 정적/자동

- `npm test` 통과.
- `npm run build` 통과.
- 시작 덱에 숙련존이 정확히 네 종류인지 확인.
- 타자 실제 경로와 자동시뮬이 모두 `resolveShowdownContact()`를 호출하는지 검색.
- 공격력에 연속 READ 배율을 곱하는 코드가 없는지 확인.
- 게임 시작 시 무조건 `startAiPitch()`를 예약하는 코드가 없는지 확인.

### 직접 플레이

- 새 게임에서 동료 1→2→3번 이후 내 4번 타석에서 자동 진행이 멈춘다.
- 카드가 없는 존을 격자에서 눌러 BASIC SWING이 된다.
- 숙련 READ가 BASIC READ보다 체감상 강하지만 성공이 확정적이지 않다.
- 인접 2존 적중은 `COVERED`이고 DEEP READ가 아니다.
- POWER는 정확한 READ에서 장타 가치가 높고 오독 시 헛스윙이 많다.
- CONTACT는 장타를 희생해 NEAR READ를 살린다.
- CUT은 2스트라이크에서 일부 실패를 파울로 바꾼다.
- ROOKIE의 2스트라이크 중하 Tell을 몇 타석 뒤 발견할 수 있다.
- ADAPTER/FOX가 현재 카드가 아니라 과거 노림에만 반응한다.
- 화면 PUBLIC과 실제 행동의 차이를 숫자 누설 없이 추론할 수 있다.

### 재미 검증 질문

1. 최고 확률이 아닌 존을 의도적으로 고른 순간이 있었나?
2. 적중했을 때 “운이 좋았다”보다 “읽었다”는 감정이 컸나?
3. 실패했을 때 다음 공의 역심리를 생각하게 됐나?
4. POWER 난사가 합리적 지배전략이었나? 그렇다면 수치가 아니라 CQ/PQ 구조부터 재검토한다.

## 10. 다음 작업 우선순위

1. 1타자 vs 1투수 짧은 코어 테스트 모드로 실제 인간 3인 플레이테스트.
2. 고정 시드 정책봇(Random / MaxProb / Pattern / PowerSpam) 추가와 지배전략 회귀 기준 수립.
3. Tell 발견 연출, 히트스톱, 사운드 보강. 신규 카드 대량 추가는 그 이후.

## 11. Visual & Impact Pass 01

2026-08-31에 코어 타석의 시각 계층을 1차 개편했다.

- 데스크톱 전투 화면을 **투수·존(왼쪽) / 카드 베팅(오른쪽)** 2열 구조로 변경했다.
- 존 격자와 카드를 확대하고 전투 전체 폭을 넓혀, 카드 패널이 작은 세로 HUD처럼 보이던 문제를 줄였다.
- 신규 경기장 배경 `assets/stadium-combat-bg-v1.png`을 역할 선택 및 경기 화면에 연결했다.
- `READ`, `DEEP READ`, `COVERED`, `NEAR READ`, `MISREAD`, `CHASE`가 서로 다른 판정 문구로 표시된다.
- DEEP READ에 전용 화면 감광, 중앙 광선, 긴 타이포그래피, 2단 저음을 추가했다.
- POWER가 실제 접촉에 성공했을 때만 저역 타격음을 한 겹 더 재생한다.
- 구형 문구인 “연속 성공 시 타구 강화”를 제거하고 “투수의 반복을 관찰 중”으로 교체했다. 연속 기록은 공격 배율이 아니다.
- 역할 선택 화면의 로고, 선택 카드, 패널 크기와 대비를 강화했다.

다음 시각 작업은 여러 이미지를 한꺼번에 교체하지 않는다. `DEEP READ`, `POWER 정타`, `POWER 실패`, `일반 READ` 네 장면을 실제 플레이 영상으로 비교한 뒤 타자·투수 스프라이트의 해상도와 애니메이션 프레임을 교체한다.

## 12. Sprite & Animation Pass 01 — 120Hz 전투 타임라인

2026-09-01에 구형 소형 base64 스프라이트를 실제 전투 경로에서 교체했다.

- 타자: 스윙 12포즈, 헛스윙 6포즈.
- 투수: 와인드업~팔로스루 12포즈, 삼진 피니시 6포즈.
- 모든 개별 포즈는 투명 512×512 PNG이며 `assets/sprites-v2/frames/`에 있다.
- React는 포즈가 바뀌는 시점에만 이미지 상태를 갱신한다. 위치·회전·스케일·임팩트 밝기는 `requestAnimationFrame`으로 매 디스플레이 갱신마다 보간한다. 120Hz 화면에서는 120fps로 갱신되고, 60Hz 화면에서는 자연스럽게 60fps로 내려간다.
- 스윙은 접촉 키포즈에 짧은 히트스톱을 두며, 본체 뒤에 두 겹의 색수차 잔상을 표시한다.
- AI 와인드업과 릴리스, 유저 투수 릴리스, 삼진 피니시가 모두 동일 타임라인을 사용한다.
- 프레임 재생은 판정 엔진과 분리되어 있다. 애니메이션을 바꾸더라도 CQ/PQ, PUBLIC/TRUE INTENT, TURN GUARD 결과 순서를 건드리면 안 된다.

시각 회귀 확인 항목:

- 타자·투수 이미지의 `naturalWidth/naturalHeight`가 512×512이며 로딩 실패가 없다.
- 헛스윙이 `batter-miss-00`부터 `05`까지 진행하고 대기로 복귀한다.
- 일반/홈런 스윙이 `batter-swing-00`부터 `11`까지 진행한다.
- 투수는 와인드업 `pitcher-pitch-00`~`05` 후 릴리스 `00`~`11`을 재생한다.
- 삼진 시 구형 저해상도 승리 포즈가 아니라 `pitcher-strikeout-00`~`05`를 사용한다.
- 애니메이션 중 잔상은 두 장이며 종료 후 DOM에서 제거된다.
- 브라우저 콘솔 오류가 없고 `npm test`, `npm run build`가 통과한다.

## 13. Impact & Cinematic Pass 02

2026-09-01에 구형 공·배트 클로즈업 중심 컷신을 고해상도 캐릭터 중심 리플레이로 교체했다.

- 결과 컷신은 현재 재생 중인 512px 타자·투수 포즈를 그대로 사용한다. 컷신 진입 때문에 구형 저해상도 캐릭터나 배트 이미지로 되돌아가면 안 된다.
- 접촉 리플레이는 공 진입 → 접촉 히트스톱 → 결과별 타구각을 한 타임라인으로 재생한다.
- `homerun`, `great`, `hit`, `foul`, `out`은 서로 다른 충격파 팔레트를 사용한다.
- 충격파는 중심광, 확장 링, 16개 속도선, 12개 파편으로 구성된다. 외부 이미지가 아니라 CSS/SVG 계층이므로 해상도에 의존하지 않는다.
- 헛스윙은 따뜻한 접촉광 대신 푸른 공기 아크와 통과 궤적을 사용한다.
- 홈런·장타·일반 안타·실패는 서로 다른 카메라 진폭과 감쇠 시간을 사용한다.
- 배너와 컷신 ID는 서로 다른 문자열 접두사를 사용한다. 같은 밀리초에 생성돼도 React key가 충돌하면 안 된다.

브라우저 회귀 확인:

- 접촉 컷신에 `.cinematic-ball` 1개, `.cinematic-character` 1개가 있다.
- 접촉 성공 시 `.impact-burst-debris`가 12개 생성된다.
- 헛스윙 시 충격파 대신 `.cinematic-whiff-arc`가 생성된다.
- 컷신 캐릭터 src가 반드시 `assets/sprites-v2/frames/`를 가리킨다.
- 연출 종료 후 컷신과 파편 DOM이 제거된다.
- 배너와 컷신 동시 생성 시 중복 key 경고가 없어야 한다.

## 14. 자율 개발 루프 운영

2026-09-01에 대화 기억 대신 파일과 Git 커밋으로 이어지는 Windows 자율 루프를 설치했다.

- 운영 본체는 `loop/loop.sh`, Windows 진입점은 `loop/loop.ps1`이다.
- 매 바퀴는 `codex exec --ephemeral`을 새로 실행한다. `resume` 또는 `fork`를 사용하지 않는다.
- `loop/PROMPT.md`를 읽고, `docs/feedback/INBOX.md` → `docs/DESIGN.md` → `docs/STATUS.md` → `HANDOFF.md` 순으로 맥락을 복원한다.
- `loop/env.sh`에 모델, 추론 강도, 작업 사이클 상한, 바퀴 사이 대기, 최대 바퀴 수, 세션 시간 제한, 모든 실행 PATH가 있다.
- `loop/STOP`은 현재 바퀴가 끝난 뒤 정상 종료시킨다.
- 날짜별 JSONL 실행 로그와 마지막 응답은 `logs/`에 저장되며 Git에서 제외된다.
- Windows 작업 스케줄러의 `9ZONE SHOWDOWN Codex Loop` 작업은 로그인 트리거와 실패 시 1분 재시작 정책으로 등록되어 있다. 현재 상태는 **Disabled**다.
- 켜기·끄기·상태 확인은 `loop/task.ps1`의 `Start`, `Stop`, `Status`로 한다.

설치 검증은 `-SmokeTest -MaxRounds 2`로 진행했다. 실행 ID `20260901-120415-2075`의 두 바퀴는 서로 다른 thread ID를 발급받았고 모두 `exit=0`으로 끝났다. 두 세션은 PROMPT와 지정 문서만 읽었으며 소스·문서·Git을 수정하지 않았다. 실제 자율 개발은 시작하지 않았다.

## 15. Shared Engine Extraction

2026-09-02에 `resolveShowdownContact()`를 467 kB 규모의 React 본체에서 `src/game/showdown-engine.js` 순수 모듈로 분리했다.

- `BaseballSim-deck-5.jsx`의 실제 유저 타격 경로와 동료 타자 자동시뮬은 같은 import를 사용한다.
- 판정 수식, 난수 호출 순서, 반환 형태는 변경하지 않았다.
- 테스트는 React와 Tone을 로드하지 않고 공용 엔진을 직접 import한다.
- `pnpm test` 3/3과 `pnpm run build`가 통과했다. 빌드의 500 kB 초과 청크 경고는 기존 알려진 문제로 남아 있다.
- 로컬 서버 실행은 확인했지만 사용 가능한 앱 내 브라우저가 없어 이번 바퀴에서는 스크린샷 QA를 완료하지 못했다.

## 16. One-at-bat Core Playtest Mode

2026-09-02에 인간 플레이테스트를 짧게 반복할 수 있는 타자 전용 `CORE TEST` 진입점을 역할 선택 화면에 추가했다.

- `CORE TEST · 한 타석`은 유저 4번 타순으로 즉시 들어가 동료 자동 타석과 정규 이닝을 기다리지 않는다.
- 한 타석이 볼넷·삼진·아웃·안타 중 하나로 끝나면 자동 진행을 멈추고 재미 검증 네 문항을 표시한다.
- 코어 테스트에서는 랜덤 이벤트, 경험치, 레벨업, 카드 보상을 발생시키지 않는다. CQ/PQ, PUBLIC/TRUE INTENT, 카드 선택 규칙은 정규 타자 경기와 같다.
- 네 문항을 모두 답해야 결과를 저장할 수 있다. 결과에는 답변, 선택 메모, 마지막 플레이, 본 투구 기록이 포함된다.
- 결과는 브라우저 `localStorage`의 `9zone-core-test-results-v1`에 누적되며 역할 선택 화면에서 현재 건수와 JSON 다운로드를 확인할 수 있다.
- `다음 플레이어`는 상태를 초기화하고 새 한 타석의 첫 투구를 명시적으로 예약한다. 정규 경기의 TURN GUARD는 그대로 유지한다.

검증은 `pnpm test` 3/3, `pnpm run build`, `git diff --check`, 로컬 서버 HTTP 200까지 통과했다. 브라우저 런타임에 사용 가능한 브라우저가 없어 스크린샷 및 클릭 회귀는 완료하지 못했다. 다음 세션은 역할 선택 → 코어 테스트 즉시 진입 → 타석 종료 → 네 문항 저장 → 다음 플레이어 재시작 → JSON 다운로드를 실제 브라우저에서 먼저 확인한 뒤 인간 3인 테스트를 진행한다.

## 17. Fixed-seed Policy Regression

2026-09-02에 Random / MaxProb / Pattern / PowerSpam 네 정책을 고정 시드로 비교하는 회귀 도구를 추가했다.

- 실행 명령은 `pnpm run test:policy`다. 각 정책은 기본 3,000타석을 수행하며 H/PA, TB/PA, 접촉률, DEEP READ 비율을 표로 출력한다.
- 정책봇은 판정식을 복사하지 않고 실제 `resolveShowdownContact()`를 호출한다.
- PUBLIC MODEL, ROOKIE의 2스트라이크 중하 Tell, ADAPTER/FOX의 최근 노림 역이용도 `src/game/showdown-pitch-model.js`로 분리해 실제 게임과 정책봇이 함께 사용한다.
- 정책 선택 난수와 투구·판정 난수는 별도 스트림이다. Random 정책의 추가 난수 소비가 이후 판정 난수열을 바꾸면 안 된다.
- 기계적 합격 기준은 세 가지다: PowerSpam TB/PA가 최선 비파워 정책의 1.05배 이하, PowerSpam 접촉률이 MaxProb보다 0.04 이상 낮음, Pattern DEEP READ 비율이 MaxProb보다 높음.
- 최초 실행에서는 PowerSpam TB/PA 비율이 1.397로 실패했다. 원인은 POWER의 PQ 보너스가 오독에도 붙고 실제 POWER 존 카드가 공용 엔진 POWER 경로에 연결되지 않은 구조였다.
- 수정 뒤 POWER는 `READ`/`DEEP_READ`에서만 PQ 보너스를 받으며, 오독 시 CQ 페널티가 커진다. 실제 POWER 존 카드와 `강타` 수식어가 모두 이 경로를 사용한다.
- 최종 고정 시드 결과는 Random 0.547, MaxProb 0.687, Pattern 0.694, PowerSpam 0.709 TB/PA다. PowerSpam/최선 비파워 비율은 1.022이며 접촉률은 PowerSpam 0.552 대 MaxProb 0.691, DEEP READ는 Pattern 0.015 대 MaxProb 0.006이다.

검증은 `pnpm test` 7/7, `pnpm run build`, `git diff --check`, `pnpm run test:policy`를 통과했다. 앱 내 브라우저는 연결 복구 절차 뒤에도 사용 가능 목록이 비어 있어 CORE TEST의 클릭 및 스크린샷 QA는 여전히 다음 세션의 최우선 작업이다.

## 18. Core Test Result Regression

2026-09-02에 브라우저 연결 부재로 반복 미검증되던 CORE TEST 결과 경로를 순수 모듈과 자동 테스트로 고정했다.

- 질문 정의, 네 문항 완결성 판정, 결과 스냅샷 생성, 로컬 저장소 읽기/추가, JSON 직렬화는 `src/game/core-test-results.js`에 있다.
- React UI는 저장 건수, 저장 버튼 활성화, 결과 추가, JSON 다운로드에 이 모듈을 직접 사용한다. 같은 규칙을 본체에 다시 복사하지 않는다.
- `false` 응답도 유효한 답으로 보존하며, 네 문항 중 하나라도 불리언이 아니면 결과를 만들지 않는다.
- 결과에는 공개 가능한 `zone`과 `pitchId`만 복사한다. TRUE INTENT 같은 숨은 필드를 투구 기록에 추가하지 않는다.
- 손상된 JSON이나 배열이 아닌 기존 값은 빈 결과로 복구한다. 반면 저장소 접근 자체가 거부되면 다운로드를 빈 파일 성공처럼 위장하지 않고 기존 UI 오류 처리로 전파한다.
- 회귀 테스트 4개가 설문 완결성, `false` 보존, 메모 정리와 결과 스냅샷, 손상 저장값 복구, 저장소 접근 실패 전파를 검사한다.

검증은 `pnpm test` 11/11, `pnpm run build`, `git diff --check`, `pnpm run test:policy`를 통과했다. 로컬 Vite 서버도 `http://localhost:5174/`에서 기동했다. 브라우저 연결 복구 절차 뒤에도 사용 가능한 브라우저 목록이 비어 있어 실제 클릭과 스크린샷 QA는 완료하지 못했다. 다음 세션은 브라우저가 제공되는 환경에서 역할 선택 → `CORE TEST` → 타석 종료 → 네 문항 저장 → 다음 플레이어 → JSON 다운로드를 직접 확인하고 인간 3인 결과를 수집한다.

## 19. Production Bundle Split

2026-09-02에 Vite 8의 Rolldown 코드 분할 설정을 추가해 프로덕션 JavaScript의 500 kB 초과 청크 경고를 제거했다.

- `vite.config.js`의 `build.rolldownOptions.output.codeSplitting`에서 `node_modules`를 vendor 청크로 분리한다.
- 기준 빌드의 단일 JavaScript 청크 798.47 kB는 앱 312.34 kB, vendor 485.38 kB, Rolldown 런타임 0.58 kB로 나뉘었다.
- 경고 한도를 올려 숨긴 것이 아니라 실제 산출물을 분리했다. 게임 소스와 런타임 동작은 변경하지 않았다.
- 프로덕션 미리보기에서 HTML, CSS, 앱·vendor·런타임 JavaScript가 모두 HTTP 200으로 응답했다.

검증은 `pnpm test` 11/11, `pnpm run build`, `git diff --check`, `pnpm run test:policy`를 통과했다. 브라우저 연결 복구 절차와 사용 가능 유형 조회 뒤에도 목록이 비어 있어 화면 클릭과 스크린샷 QA는 이번 바퀴에도 수행하지 못했다. 다음 세션은 브라우저가 제공되는 환경에서 역할 선택 → `CORE TEST` → 타석 종료 → 네 문항 저장 → 다음 플레이어 → JSON 다운로드를 직접 확인하고 인간 3인 결과를 수집한다.

## 20. Core Test UI Entry Regression

2026-09-02에 `CORE TEST`의 역할 선택 진입 경로를 실제 React UI 클릭 회귀 테스트로 고정했다.

- `tests/core-test-ui.test.jsx`는 happy-dom에서 전체 `BaseballSim`을 렌더링하고 Testing Library로 튜토리얼 `건너뛰기`와 `CORE TEST · 한 타석` 버튼을 클릭한다.
- 역할 화면에서 저장 건수 0/3과 코어 테스트 진입 버튼이 노출되는지 확인한다.
- 진입 뒤 코어 테스트 전용 안내, 상대 투수, `① 카드 선택`이 즉시 노출되는지 확인한다. 따라서 정규 경기의 동료 1~3번 자동 타석으로 빠지는 회귀를 자동 검사에서 잡을 수 있다.
- Tone은 이 DOM 회귀에서 모듈만 격리한다. 게임 판정, PUBLIC/TRUE INTENT, 실제 컴포넌트 상태 전이는 모킹하지 않는다.
- UI 테스트 의존성은 개발 전용 `happy-dom`, `@testing-library/react`이며 프로덕션 번들에는 들어가지 않는다.

검증은 `pnpm test` 12/12, `pnpm run build`, `git diff --check`, `pnpm run test:policy`를 통과했다. 브라우저 런타임의 복구 절차와 사용 가능 유형 조회 뒤에도 목록이 비어 있어 실제 픽셀과 한 타석 종료 이후 설문 저장 → 다음 플레이어 → JSON 다운로드는 확인하지 못했다. 다음 세션은 브라우저가 제공되면 이 남은 후반 흐름과 스크린샷을 먼저 확인한 뒤 인간 3인 결과를 수집한다.

## 21. Core Test Completion UI Regression

2026-09-02에 `CORE TEST`의 한 타석 종료 이후 경로를 실제 React UI 클릭 회귀로 고정했다.

- 고정 난수에서 `지켜보기`와 `다음투구`를 실제로 눌러 4볼 볼넷으로 한 타석을 끝낸다. 제품 내부 완료 함수를 직접 호출하지 않는다.
- 설문 네 문항에 응답하고 메모를 입력한 뒤 `결과 저장`을 눌러 로컬 저장소와 화면의 1/3 건수를 함께 확인한다.
- `다음 플레이어`를 눌렀을 때 완료 화면이 사라지고 새 코어 테스트 타석이 시작되는지 확인한다.
- 새 렌더에서도 저장 건수 1/3이 복원되고 `JSON 받기`가 날짜 기반 파일명, Blob URL 생성·해제를 수행하는지 확인한다.
- happy-dom에서 진행되지 않는 애니메이션 프레임은 테스트 안에서만 16ms 타이머로 재현하며 제품 코드는 변경하지 않는다.

검증은 `pnpm test` 13/13, `pnpm run build`, `git diff --check`, `pnpm run test:policy`를 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했다. 앱 내 브라우저는 런타임 복구 문서 확인과 사용 가능 유형 조회 뒤에도 목록이 비어 있어 실제 픽셀·레이아웃 및 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 이 자동화된 후반 흐름의 화면만 확인한 뒤 인간 3인 결과를 수집한다.

## 22. Core Test Human Report Aggregation

2026-09-02에 여러 플레이어가 내려받은 누적 CORE TEST JSON을 한 번에 검토하는 보고서 경로를 추가했다.

- 실행 명령은 `pnpm run report:core-test -- <export.json> [more-exports.json ...]`다.
- 각 파일은 누적 배열이어야 하며 배열이 아니면 CLI가 오류로 종료한다. 배열 안에서는 네 문항이 모두 불리언인 응답만 집계하고 손상된 항목은 건너뛴다.
- 같은 누적 내보내기를 여러 번 전달해도 완전히 같은 응답은 한 번만 센다. 결과는 타임스탬프 순으로 정렬한다.
- 네 질문별 yes/no와 긍정 신호를 함께 출력한다. `powerDominant`만 `아니오`가 긍정 신호이고 나머지 세 질문은 `예`가 긍정 신호다.
- 완전한 응답이 3개 이상이면 `READY`, 미만이면 `MORE NEEDED`를 표시하며 자유 메모도 함께 출력한다.
- 병합과 요약은 `src/game/core-test-results.js`의 순수 함수다. CLI와 테스트가 같은 구현을 사용하고 별도 집계식을 복사하지 않는다.

검증은 `pnpm test` 15/15, `pnpm run build`, `git diff --check`를 통과했다. 두 누적 JSON을 사용한 실제 CLI 실행에서 응답 3건, 중복 제거 1건, 네 신호와 메모 출력을 확인했다. 이 JSON은 CLI 검증용 입력이므로 인간 플레이테스트 결과로 취급하지 않는다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 복구 절차 뒤에도 사용 가능한 브라우저가 없어 픽셀·스크린샷 QA는 진행하지 못했다. 다음 세션은 브라우저 화면을 먼저 확인하고 실제 인간 3명의 JSON을 수집해 이 명령으로 집계한다.

## 23. Core Test Order-independent Deduplication

2026-09-02에 누적 CORE TEST JSON의 동일 응답 판정을 객체 키 순서와 무관하게 고쳤다.

- 이전 중복 지문은 `JSON.stringify(result)`를 그대로 사용해, 같은 응답도 JSON 포매터가 최상위 또는 `answers` 키 순서를 바꾸면 별도 응답으로 집계될 수 있었다.
- 이제 객체 키를 모든 깊이에서 정렬한 뒤 지문을 만든다. 배열 순서는 투구 기록처럼 의미가 있으므로 유지한다.
- 회귀 테스트는 최상위 필드와 네 답변의 키 순서를 역순으로 만든 동일 응답이 중복 1건으로 제거되고 최초 응답만 남는지 확인한다.
- 이 변경은 내보내기 형식, 유효 응답 판정, 시간순 정렬, 요약 기준을 바꾸지 않는다.

검증은 대상 회귀 6/6, 전체 `pnpm test` 15/15, `pnpm run build`, `pnpm run test:policy`, `git diff --check`를 통과했다. 코드 커밋은 `a8a189a`다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 뒤에도 사용 가능 목록이 비어 실제 픽셀·레이아웃 및 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 자동화된 CORE TEST 후반 화면을 먼저 확인한 뒤 인간 3명의 실제 JSON을 수집한다.

## 24. Core Game Design Baseline

2026-09-02에 비어 있던 `docs/DESIGN.md`를 이 인수인계서에서 이미 확정한 제품 가설로 채웠다.

- 한 문장 정의와 핵심 감정을 “확률 선택”이 아닌 “투수를 읽고 위험을 감수한 존 베팅”으로 고정했다.
- READ → BET → REVEAL → IMPACT를 제품의 핵심 플레이 흐름으로 명시했다.
- READ와 HIT의 분리, PUBLIC/TRUE INTENT 정보 비대칭, 정확한 READ에서만 강한 POWER, BASIC SWING, 공용 판정 엔진, 인간 플레이테스트 우선 원칙을 설계 기준으로 올렸다.
- TRUE INTENT 노출, 연속 READ 공격 배율, 현재 선택을 엿보는 AI, 판정식 복사, 검증 전 콘텐츠 대량 추가를 명시적으로 금지했다.
- 새 규칙을 발명하거나 런타임 코드를 바꾸지 않았으며 커밋은 `8a9b1ff`다.

문서 변경은 `git diff --check`를 통과했다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 연결 복구 절차 뒤 사용 가능한 브라우저 유형이 0개라 CORE TEST 후반 화면의 픽셀·스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 그 화면을 먼저 확인한 뒤 인간 3명의 실제 JSON을 수집한다.

## 25. Core Test Export Schema Validation

2026-09-02에 CORE TEST 인간 보고서가 답변만 갖춘 임의 객체를 실제 앱 응답으로 세던 입력 검증 빈틈을 막았다.

- `isValidCoreTestResult()`가 version 1, 앱 형식의 정규 ISO timestamp, 네 불리언 답변, 문자열 메모, nullable 최종 플레이, 공개 투구 기록 배열을 함께 검증한다.
- 투구 기록은 0~8 스트라이크존과 9 유인구만 허용하며 비어 있지 않은 문자열 pitch ID를 요구한다.
- 병합과 요약은 같은 검증 함수를 사용한다. 유효하지 않은 객체가 병합을 우회해 `READY` 응답 수에 들어가면 안 된다.
- 기존 앱이 생성하는 내보내기 형식은 바꾸지 않았다. 필수 필드가 빠진 수동·손상 객체만 보고서에서 제외한다.
- 회귀 테스트는 정상 내보내기와 누락 필드, 잘못된 버전·시각·메모·존을 확인한다.

검증은 대상 회귀 7/7, 전체 `pnpm test` 16/16, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했다. 기존 검증용 미추적 JSON 두 파일의 불완전한 객체 4건은 CLI에서 모두 무효로 건너뛰어 `0/3 (MORE NEEDED)`가 됐다. 코드 커밋은 `665aafb`다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 뒤 사용 가능 목록이 비어 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 CORE TEST 후반 화면을 먼저 확인한 뒤 앱에서 내려받은 완전한 실제 인간 3인 JSON을 수집한다.

## 26. Core Test Exact Public Schema Validation

2026-09-02에 CORE TEST 보고서 입력 검증을 필수 필드 확인에서 앱 공개 스키마의 정확 일치 검사로 강화했다.

- 최상위 결과는 `version`, `timestamp`, `answers`, `note`, `finalPlay`, `pitchesSeen`만 허용한다.
- 답변 객체는 네 질문 ID만, 각 투구 기록은 `zone`과 `pitchId`만 허용한다. 따라서 수동 JSON에 `hiddenIntent`, `hiddenTell`, `trueDist` 같은 필드를 섞어도 앱 내보내기로 인정하지 않는다.
- 구종 ID는 실제 앱이 생성하는 `fastball`, `slider`, `curve`, `change`만 허용한다.
- 앱의 `createCoreTestResult()` 출력 형식과 version 1 저장 형식은 바꾸지 않았다.
- 회귀 테스트는 최상위·답변·투구의 추가 필드와 존재하지 않는 구종을 모두 거부하는지 확인한다.

검증은 전체 `pnpm test` 16/16, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했다. 기존 미추적 검증용 JSON 두 파일의 객체 4건은 CLI에서 모두 무효로 건너뛰어 `0/3 (MORE NEEDED)`가 됐다. 코드 커밋은 `d04a444`다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 뒤 사용 가능한 브라우저 유형이 0개라 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 CORE TEST 후반 화면을 먼저 확인한 뒤 앱에서 내려받은 완전한 실제 인간 3인 JSON을 수집한다.

## 27. Core Test Non-empty Pitch History Validation

2026-09-02에 CORE TEST 보고서가 투구 기록이 없는 수동 객체를 실제 완료 응답으로 세던 입력 검증 빈틈을 막았다.

- 실제 앱에서는 한 타석이 끝나야 설문을 저장할 수 있으므로 완료 응답에는 최소 한 공의 기록이 반드시 있다.
- `isValidCoreTestResult()`는 이제 `pitchesSeen`이 배열인지만 보지 않고 원소가 최소 1개인지도 검사한다.
- `pitchesSeen: []`인 version 1 형식 객체가 무효로 거부되는 회귀 테스트를 추가했다.
- 정상 앱 내보내기 스키마와 집계·중복 제거·요약 동작은 바꾸지 않았다.

검증은 전체 `pnpm test` 16/16, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했고 코드 커밋은 `4310731`이다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 절차 뒤 사용 가능한 브라우저 목록이 비어 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 자동화된 CORE TEST 후반 화면을 먼저 확인한 뒤 앱에서 내려받은 실제 인간 3인 JSON을 수집한다.

## 28. Core Test Local Storage Validation

2026-09-02에 CORE TEST 로컬 저장소의 배열 안에 손상된 항목이 섞였을 때 화면 카운트와 다운로드가 실제 보고서 집계와 어긋나는 문제를 막았다.

- `parseCoreTestResults()`는 이제 배열 여부만 확인하지 않고 각 항목에 `isValidCoreTestResult()`를 적용한다.
- 역할 선택 화면의 저장 건수, 새 응답을 추가할 때 읽는 기존 결과, `JSON 받기` 직렬화가 모두 같은 정제 경로를 사용한다.
- 새로 추가하는 결과도 앱 스키마를 통과하지 못하면 `appendCoreTestResult()`가 저장 전에 거부한다.
- 유효 응답과 손상 응답이 섞인 저장값에서 유효 응답만 읽고, 다음 저장 시 손상 항목을 제거하며, 다운로드에도 유효 응답만 남기는 회귀 테스트를 추가했다.
- 보고서 CLI의 입력 검증과 version 1 내보내기 형식은 바꾸지 않았다.

검증은 전체 `pnpm test` 17/17, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 구현 커밋은 `9f755e8`이다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 절차 뒤 사용 가능한 브라우저 유형이 0개라 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 자동화된 CORE TEST 후반 화면을 먼저 확인한 뒤 앱에서 내려받은 실제 인간 3인 JSON을 수집한다.

## 29. Core Test Save Error Feedback

2026-09-02에 CORE TEST 완료 설문에서 로컬 저장소 쓰기가 실패해도 사용자에게 오류가 보이지 않던 문제를 막았다.

- 이전에는 저장 예외가 공용 `message`만 바꿨지만, 완료 설문이 열린 동안 그 메시지를 표시하는 전투 화면은 숨겨져 있었다.
- 완료 설문 전용 `coreTestSaveError` 상태를 추가하고 설문 안에 `role="alert"` 오류를 표시한다.
- 새 저장 시도 직전과 다음 CORE TEST 시작 때 오류를 비운다. 실패 뒤에는 답변과 메모를 유지하므로 저장소 상태를 해결한 뒤 같은 화면에서 다시 시도할 수 있다.
- UI 회귀 테스트는 `localStorage.setItem()`이 한 번 실패하도록 만든 뒤 오류가 보이고 `기록 완료`로 넘어가지 않는지 확인한다. 저장소를 복구한 다음 같은 설문을 재저장해 다음 플레이어와 JSON 다운로드까지 기존 후반 흐름이 유지되는지도 확인한다.

검증은 전체 `pnpm test` 17/17, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 코드 커밋은 `7439e52`다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 CORE TEST 완료 설문의 정상 저장과 실패 경고를 화면에서 먼저 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 30. Core Test Export Error Feedback

2026-09-02에 역할 선택 화면의 `JSON 받기`가 실패해도 사용자에게 오류가 보이지 않던 문제를 막았다.

- 이전에는 내보내기 예외가 공용 전투 `message`만 바꿨지만, 역할 선택 화면은 그 메시지를 표시하지 않아 버튼이 무반응처럼 보였다.
- JSON 내보내기 전용 오류 상태를 추가하고 저장 건수 바로 아래에 `role="alert"` 오류를 표시한다.
- 재시도 직전에 오류를 비운다. 실패해도 로컬 저장 결과는 바꾸지 않으며, 다운로드 환경이 복구되면 같은 버튼으로 다시 내보낼 수 있다.
- UI 회귀 테스트는 Blob URL 생성이 한 번 실패할 때 오류가 보이고 다운로드가 발생하지 않는지 확인한 뒤, 같은 버튼을 다시 눌러 오류 제거·날짜 기반 파일명·Blob URL 해제를 검증한다.
- 저장과 내보내기에서 같은 숨은 오류가 두 번 반복됐으므로, 사용자 작업 실패는 현재 화면에 접근 가능하게 표시하고 재시도를 회귀 검사로 고정한다는 규칙을 `loop/PROMPT.md`에 올렸다.

검증은 전체 `pnpm test` 17/17, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `97436de`다. 로컬 앱은 `http://127.0.0.1:5174/`에서 기동했지만 브라우저 연결 복구 뒤 사용 가능한 브라우저 유형이 0개라 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 CORE TEST 완료 설문의 정상·실패 저장과 역할 화면의 정상·실패 JSON 내보내기를 화면에서 먼저 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 31. Core Test Error Feedback Follow-ups

### 31.2 Partial Download Cleanup

2026-09-02에 CORE TEST JSON 내보내기가 Blob URL 생성 이후 링크 클릭 단계에서 실패할 때 임시 자원이 남던 문제를 막았다.

- 이전에는 링크 클릭이 예외를 던지면 역할 선택 화면에 오류는 표시됐지만, 임시 다운로드 링크가 DOM에 남고 생성된 Blob URL도 해제되지 않았다.
- `downloadCoreTestResults()`는 이제 링크와 Blob URL을 `finally`에서 최선으로 정리한다. 생성 전 실패, 클릭 중 실패, 정상 다운로드가 같은 정리 경로를 사용한다.
- 실패해도 저장된 CORE TEST 결과는 바꾸지 않으며, 오류가 보이는 같은 역할 선택 화면에서 `JSON 받기`를 다시 누를 수 있다.
- UI 회귀 테스트는 첫 링크 클릭을 실패시킨 뒤 접근 가능한 오류, 임시 링크 제거, 실패 Blob URL 해제를 확인한다. 두 번째 클릭은 날짜 기반 파일명으로 성공하며 성공 Blob URL과 임시 링크도 정리되는지 확인한다.

검증은 대상 CORE TEST UI 3/3, 전체 `pnpm test` 18/18, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `8bb26c8`이다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 실제 픽셀·레이아웃과 스크린샷 QA를 수행하지 못했다. 다음 세션은 브라우저가 제공되면 자동화된 CORE TEST 후반 흐름을 화면에서 먼저 확인한 뒤 앱에서 내려받은 실제 인간 3인 JSON을 수집한다.

### 31.1 Initial Load Error Feedback

2026-09-02에 역할 선택 화면이 CORE TEST 로컬 저장소를 처음 읽지 못했을 때 기존 결과가 없는 것처럼 `0/3`만 표시하던 문제를 막았다.

- 기존의 관대한 `readCoreTestResults()`는 손상된 저장값 복구용으로 유지하고, 저장소 접근 실패를 호출자가 구분할 수 있는 `readCoreTestResultsStrict()`를 추가했다.
- 역할 선택 화면은 초기 읽기 실패를 `role="alert"`로 표시하고 `다시 불러오기` 버튼을 제공한다.
- 실패 뒤에도 화면을 떠나거나 기존 데이터를 덮지 않는다. 저장소가 복구되면 같은 화면에서 실제 저장 건수를 다시 읽고 오류를 지운다.
- 새 결과 저장에 성공했을 때도 초기 불러오기 오류를 지우고 최신 건수를 표시한다.
- UI 회귀 테스트는 기존 1건이 있는 상태에서 첫 읽기만 실패시킨 뒤 경고와 임시 `0/3`을 확인하고, 재시도로 `1/3`을 복원한다.

검증은 전체 `pnpm test` 18/18, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `bb2b0c4`다. 로컬 앱은 `http://127.0.0.1:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 실제 픽셀·레이아웃과 스크린샷 QA는 수행하지 못했다. 다음 세션은 브라우저가 제공되면 CORE TEST 완료 설문의 정상·실패 저장, 초기 결과 불러오기 재시도, 정상·실패 JSON 내보내기를 화면에서 먼저 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 32. General Feedback Save Error Feedback

2026-09-02에 일반 피드백 제출이 실패할 때 열린 모달에서는 오류를 볼 수 없던 문제를 막았다.

- 이전에는 저장 예외가 뒤쪽 경기 화면의 공용 `message`만 바꿔, 피드백 모달에서는 제출이 무반응처럼 보였다.
- 이제 제출 실패를 모달 안의 `role="alert"`로 표시한다.
- 실패 뒤 평점과 자유 입력을 그대로 유지하며, 재시도 직전에 이전 오류를 비운다. 모달을 새로 열 때도 지난 실패 문구를 남기지 않는다.
- UI 회귀 테스트는 공유 저장소 쓰기가 한 번 실패할 때 경고, 입력 문장, 5점 선택이 유지되는지 확인하고 같은 제출 버튼의 두 번째 시도가 성공 화면으로 전환되는지 검사한다.

검증은 대상 UI 4/4, 전체 `pnpm test` 19/19, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `a791511`이다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 실제 픽셀·레이아웃과 스크린샷 QA를 수행하지 못했다. 다음 세션은 브라우저가 제공되면 피드백 정상·실패 제출과 자동화된 CORE TEST 후반 흐름을 화면에서 먼저 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 33. Feedback Export Load Error Recovery

2026-09-02에 관리자 피드백 내보내기 패널이 공유 저장소 목록을 읽지 못했을 때 잘못된 JSON을 내려받을 수 있던 문제를 막았다.

- 이전에는 목록 읽기 실패를 `exportData`의 문자열 `불러오기 실패`로 저장해 패널이 `0건`처럼 보였고, 사용자가 그 문자열을 `.json` 파일로 다운로드할 수도 있었다.
- 이제 실패를 별도 `exportLoadError` 상태로 관리하고 열린 패널 안에 `role="alert"` 오류와 `다시 불러오기` 버튼을 표시한다.
- 재시도 직전에 오류를 비우고, 성공하면 실제 피드백 목록과 건수를 복원한다. 오류가 남아 있는 동안에는 파일 다운로드를 비활성화한다.
- 이전에 성공적으로 불러온 데이터가 있더라도 이후 새로고침 실패가 그 데이터를 오류 문자열로 덮어쓰지 않는다.
- UI 회귀 테스트는 첫 목록 읽기 실패의 접근 가능한 오류, 잘못된 문자열 미노출, 다운로드 차단을 확인하고, 두 번째 읽기 성공 뒤 1건의 실제 JSON과 다운로드 재활성화를 검사한다.

검증은 전체 `pnpm test` 20/20, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `c1f21ab`이다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했다. 로컬 앱은 `http://localhost:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 관리자 내보내기 패널의 실제 픽셀·레이아웃과 스크린샷 QA를 수행하지 못했다. 다음 세션은 브라우저가 제공되면 이 패널과 자동화된 CORE TEST 후반 흐름을 먼저 화면 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 34. Feedback Download Error Recovery

2026-09-02에 관리자 피드백 JSON 다운로드가 실패할 때 패널이 무반응으로 남고 임시 자원이 누수될 수 있던 문제를 막았다.

- `파일로 다운로드`는 이제 클릭 직전에 이전 오류를 비우고, 실패하면 열린 내보내기 패널 안에 `role="alert"` 오류를 표시한다.
- 실패해도 이미 불러온 피드백 JSON과 수집 건수는 유지하므로 다운로드 환경을 해결한 뒤 같은 버튼으로 다시 시도할 수 있다.
- Blob URL 생성 뒤 링크 클릭이 실패해도 임시 링크와 Blob URL을 `finally`에서 최선으로 정리한다. 정상 다운로드도 같은 정리 경로를 사용한다.
- UI 회귀 테스트는 첫 클릭 실패의 접근 가능한 오류, 기존 데이터 보존, 임시 링크 제거, 실패 Blob URL 해제를 확인한다. 두 번째 클릭은 정상 파일명으로 성공하며 오류와 임시 자원을 남기지 않는지 검사한다.

검증은 전체 `pnpm test` 21/21, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `a079689`이다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했다. 로컬 앱은 `http://127.0.0.1:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 관리자 내보내기 패널의 실제 픽셀·레이아웃과 스크린샷 QA를 수행하지 못했다. 다음 세션은 브라우저가 제공되면 이 패널과 자동화된 CORE TEST 후반 흐름을 먼저 화면 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 35. Feedback Partial Export Guard

2026-09-02에 관리자 피드백 목록 중 개별 항목 하나의 읽기나 JSON 파싱이 실패해도 나머지만 정상 내보내기로 제공하던 데이터 손실 경로를 막았다.

- 목록이 반환한 모든 키의 값이 존재하고 JSON 파싱에 성공해야만 `exportData`를 새 완전한 결과로 확정한다.
- 개별 항목 하나라도 실패하면 열린 내보내기 패널의 기존 `role="alert"` 오류와 `다시 불러오기` 경로를 사용하며, 부분 JSON 다운로드를 비활성화한다.
- 재시도는 목록과 각 항목을 처음부터 다시 읽는다. 모든 항목이 복구되면 정확한 수집 건수와 완전한 JSON을 표시하고 다운로드를 다시 활성화한다.
- 이전에 성공적으로 불러온 데이터가 있다면 실패한 새로고침이 그 내용을 부분 데이터로 덮어쓰지 않는다.
- UI 회귀 테스트는 두 항목 중 두 번째 읽기를 한 번 실패시켜 오류·빈 부분 결과·다운로드 차단을 확인하고, 같은 패널의 재시도로 두 항목 전체와 다운로드 가능 상태가 복원되는지 검사한다.

검증은 대상 UI 7/7, 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 통과했으며 구현 커밋은 `2aa44d9`이다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022로 기존 기준을 유지했다. 로컬 앱은 `http://127.0.0.1:5174/`에서 기동했지만 브라우저 연결 복구 절차와 사용 가능 유형 조회 결과가 0개라 개별 항목 실패·재시도 상태의 실제 픽셀·레이아웃과 스크린샷 QA를 수행하지 못했다. 다음 세션은 브라우저가 제공되면 관리자 내보내기 패널과 자동화된 CORE TEST 후반 화면을 먼저 확인한 뒤 실제 인간 3인의 JSON을 수집한다.

## 36. Core Test Completion Visual QA Checkpoint

2026-09-02에 자동 회귀가 덮고 있는 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 픽셀·레이아웃 QA를 한 가지 작업으로 시도했다.

- 작업 전 전체 `pnpm test` 22/22가 통과했다.
- Vite 앱은 `http://127.0.0.1:5174/`에서 정상 기동했다.
- 브라우저 런타임 선택이 `No browser is available`로 실패했다. 지정된 연결 복구 절차에 따라 사용 가능한 브라우저 유형을 한 번 조회했으며 결과는 빈 목록 `[]`였다.
- 화면을 직접 보지 못했으므로 스크린샷을 만들거나 픽셀·레이아웃을 합격 처리하지 않았다. 별도 브라우저 자동화로 우회하지 않았고 제품 코드도 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 같은 URL에서 이 후반 흐름만 먼저 확인한다. 화면 결함이 보이면 결함 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 37. Browser Plugin Recovery Checkpoint

2026-09-02의 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 다시 한 가지 작업으로 시도했다.

- 작업 전 전체 `pnpm test` 22/22가 통과했다.
- 브라우저 플러그인의 로컬 URL 선택을 처음 초기화할 때 Windows 샌드박스 ACL 오류가 한 번 발생했다. 연결 상태 재확인 뒤 같은 런타임 초기화는 성공했지만 브라우저 선택은 `No browser is available`로 끝났다.
- 지정된 부트스트랩 복구 안내를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번 조회했으며 결과는 빈 목록 `[]`였다. 다른 브라우저 자동화 도구로 우회하지 않았다.
- Vite 앱은 `http://127.0.0.1:5174/`에서 기동했고 HTML 요청은 HTTP 200을 반환했다.
- 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았고 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 38. Independent Browser Availability Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 작업 전 전체 `pnpm test` 22/22가 통과했다.
- Vite 앱은 `http://127.0.0.1:5174/`에서 정상 기동했고 HTML 요청은 HTTP 200을 반환했다.
- 로컬 URL의 브라우저 선택이 `No browser is available`로 실패했다. 지정된 부트스트랩 복구 안내를 읽고 기존 런타임에서 사용 가능한 브라우저 유형을 한 번 조회했으며 결과는 빈 목록 `[]`였다.
- 별도 브라우저 자동화로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 39. Browser Skill Availability Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 작업 전 전체 `pnpm test` 22/22가 통과했다.
- Vite 앱은 `http://127.0.0.1:5174/`에서 정상 기동했고 HTML 요청은 HTTP 200을 반환했다.
- Browser 스킬 지침에 따라 로컬 URL에 맞는 브라우저를 선택했지만 `No browser is available`로 실패했다.
- 지정된 부트스트랩 복구 안내를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 전체 `pnpm test`, `pnpm run test:policy`, `pnpm run build`, `git diff --check`를 다시 통과시킨 뒤 이 상태를 문서 체크포인트로 남겼다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 40. Browser Runtime Recheck Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 작업 전 전체 `pnpm test` 22/22가 통과했다.
- Vite 앱은 `http://localhost:5173/`에서 정상 기동했고 HTML 요청은 HTTP 200을 반환했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 안내에 따라 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 41. Browser Runtime Availability Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 작업 전 전체 `pnpm test` 22/22가 통과했다.
- Vite 앱은 `http://localhost:5173/`에서 정상 기동했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 안내를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 42. Pre-visual Commit and Browser Availability Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `7c40444`로 고정했다. 미추적 검증용 JSON 두 파일과 런처 관리 `logs/`는 포함하지 않았다.
- Vite 앱은 `http://127.0.0.1:5173/`에서 기동했고 HTML 요청은 HTTP 200을 반환했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 안내를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 43. Independent Visual QA Retry Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `3f5ec97`로 고정했다. 기존 미추적 검증용 JSON 두 파일과 런처 관리 `logs/`는 포함하지 않았다.
- Vite 앱은 `http://127.0.0.1:5173/`에서 정상 기동했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 절차를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 44. Local Browser Discovery Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `f46af53`으로 고정했다. 기존 미추적 검증용 JSON 두 파일과 런처 관리 로그는 포함하지 않았다.
- Vite 앱은 `http://localhost:5173/`에서 정상 기동했고 HTML 요청은 HTTP 200을 반환했으며 `#root` 마운트 지점이 존재했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 절차를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 45. Fresh Browser Availability Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `56a2e36`으로 고정했다. 기존 미추적 검증용 JSON 두 파일과 런처 관리 로그는 포함하지 않았다.
- Vite 앱은 `http://127.0.0.1:5173/`에서 정상 기동했고 HTML 요청은 HTTP 200을 반환했으며 `#root` 마운트 지점이 존재했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 절차를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 46. Current Browser Availability Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `24dc067`로 고정했다. 기존 미추적 검증용 JSON 두 파일과 런처 관리 로그는 포함하지 않았다.
- Vite 앱은 `http://127.0.0.1:5173/`에서 정상 기동했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 절차를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 47. Latest Independent Visual QA Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `629c690`으로 고정했다. 기존 미추적 검증용 JSON 두 파일과 런처 관리 로그는 포함하지 않았다.
- Vite 앱은 `http://localhost:5173/`에서 정상 기동했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 절차를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.

## 48. Current Independent Visual QA Checkpoint

2026-09-02의 새 독립 회차에서 CORE TEST 타석 종료 → 설문 저장 → 다음 플레이어 → JSON 받기 흐름의 실제 화면 QA를 한 가지 작업으로 다시 시도했다.

- 화면 확인 전 전체 `pnpm test` 22/22, `pnpm run test:policy`, `pnpm run build`, `git diff --check`가 통과했다. 정책 수치는 PowerSpam/최선 비파워 TB/PA 1.022이며 세 기준이 모두 참이다.
- 검사 통과 상태를 화면 보기 전에 빈 체크포인트 커밋 `752777a`로 고정했다. 기존 미추적 검증용 JSON 두 파일과 런처 관리 로그는 포함하지 않았다.
- Vite 앱은 `http://localhost:5173/`에서 정상 기동했고 HTML 요청은 HTTP 200을 반환했으며 `#root` 마운트 지점이 존재했다.
- 로컬 URL의 브라우저 선택은 `No browser is available`로 실패했다. Browser 스킬의 지정 복구 절차를 읽고 기존 런타임을 재사용해 사용 가능한 브라우저 유형을 한 번만 조회했으며 결과는 빈 목록 `[]`였다.
- 다른 브라우저 자동화 도구로 우회하지 않았고, 화면을 직접 보지 못했으므로 스크린샷이나 픽셀·레이아웃을 합격 처리하지 않았다. 제품 코드는 변경하지 않았다.
- 다음 세션은 사용 가능한 브라우저가 있을 때 이 후반 흐름만 먼저 확인한다. 결함이 보이면 하나만 수정하고 회귀 검사를 추가한 뒤 인간 3인 플레이테스트로 넘어간다.
