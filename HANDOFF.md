# 9ZONE SHOWDOWN — Codex 인수인계

## 1. 제품 가설

이 게임의 핵심 감정은 “확률이 높은 곳을 눌렀다”가 아니라 **“내가 저 투수를 읽었다”**다. 따라서 기능 수나 카드 수보다 아래 한 장면을 먼저 검증한다.

> 공개 확률은 낮지만 앞선 투구에서 발견한 습관을 믿고 특정 존에 POWER를 건다. 실제 공이 그 존에 들어오고 `DEEP READ`가 뜬 뒤 장타가 날 수 있다. 틀리면 큰 대가를 치른다.

봇 시뮬레이션은 지배전략을 찾는 도구이지 재미의 증명이 아니다. 최종 판단은 인간 플레이테스트로 한다.

## 2. 코어 루프: READ → BET → REVEAL → IMPACT

- **READ**: PUBLIC 확률, 카운트, 앞선 투구 기록, 투수 성향으로 위치를 예측한다.
- **BET**: 존 1장, 인접 존 2장, 존+수식어 또는 카드가 없는 존의 BASIC SWING을 선택한다.
- **REVEAL**: 이미 TRUE INTENT에서 샘플링된 실제 존을 공개한다. `READ`, `DEEP READ`, `COVERED`, `NEAR READ`, `MISREAD`, `CHASE`를 판정한다.
- **IMPACT**: READ 결과·숙련·수식어를 CQ에 넣어 접촉을 먼저 판정한다. 접촉한 뒤 PQ로 단타/장타/홈런을 판정한다.

READ는 HIT와 동의어가 아니다. 완벽히 읽고도 헛스윙할 수 있고, 잘못 읽고도 운 좋은 안타가 나올 수 있어야 한다.

## 3. 시작 4숙련존과 BASIC SWING

시작 숙련존은 `[중상(1), 좌중(3), 정중(4), 중하(7)]` 네 곳이다. 이전의 9존 시작 루프를 되살리지 않는다.

손패에 해당 존 카드가 없어도 스트라이크존 격자를 눌러 BASIC SWING을 할 수 있다. BASIC은 카드를 소모하지 않지만 비숙련 CQ/PQ 페널티를 받는다. 목적은 “카드가 없어서 아무것도 못 함”을 없애되 덱 성장의 의미는 유지하는 것이다.

## 4. CQ/PQ 판정

`resolveShowdownContact()`가 실제 플레이와 자동시뮬의 공용 엔진이다.

- **CQ(Contact Quality)**: READ 정확도, 숙련 여부, CONTACT/POWER/CUT, 2존 커버, 투수 구위, 분산으로 접촉 여부를 결정한다.
- **PQ(Power Quality)**: 접촉 이후 READ, 숙련, POWER, 2존 커버, 분산으로 타구 등급을 결정한다.
- **POWER**: CQ를 낮추고 PQ를 크게 올린다. 정확히 읽지 못하면 강하지 않다.
- **CONTACT**: CQ를 높이고 PQ를 낮춘다.
- **CUT**: 일부 실패를 파울로 바꾸어 생존 시간을 산다.
- **2존 COVERED**: 접촉 보험이지만 PERFECT/DEEP READ가 아니며 장타가 약하다.

구형 `resolveContact()`는 투수 역할 등 아직 남은 레거시 경로를 위한 호환 코드다. 타자 코어와 동료 타자 자동시뮬은 반드시 `resolveShowdownContact()`를 사용한다.

## 5. PUBLIC MODEL vs TRUE INTENT

- **PUBLIC**: 플레이어에게 보여 주는 분포. 일반 제구 모델과 관찰 노이즈 기반이다.
- **TRUE INTENT**: AI 단계, 카운트, 숨은 습관, 과거 플레이어 행동을 반영한 실제 샘플링 분포다.
- TRUE 수치는 UI에 직접 노출하지 않는다. 투구 후에도 숫자를 공개하지 않고 `DEEP READ` 피드백만 준다.
- 낮은 PUBLIC 확률을 우연히 맞힌 것만으로 DEEP READ를 주지 않는다. 선택 존의 TRUE가 PUBLIC보다 의미 있게 높아야 한다.

## 6. ROOKIE / ADAPTER / FOX

- **ROOKIE**: 읽을 수 있는 강한 Tell. 현재 구현은 2스트라이크에서 중하 승부구를 강하게 선호한다.
- **ADAPTER**: Tell을 약화하고 최근 네 번의 플레이어 노림 존을 피한다.
- **FOX**: Tell을 더 약화하고 최근 여덟 번의 노림을 더 강하게 역이용한다.

AI는 `playerAimHistoryRef`의 완료된 과거 선택만 본다. `selectedIdx`, `comboRef`의 현재 선택을 투구 결정에 사용하면 안 된다.

## 7. TURN GUARD

유저 타자는 4번 타순이다. 시작 직후 AI 투구를 강제 호출하지 않는다. 1~3번 동료 타석을 자동 처리하고 유저 슬롯 진입 effect가 첫 투구를 시작한다.

자동시뮬 함수 첫 줄에서 현재 타순 ref를 다시 검사한다. 이미 예약된 타이머가 늦게 실행되더라도 유저 슬롯이면 즉시 종료한다. 자동 타석 종료 후 다음 타자가 유저라면 다음 자동 타이머를 예약하지 않는다.

## 8. 자동시뮬 일치 원칙

과거 가장 반복된 사고는 “밸런스 시뮬레이터의 규칙과 실제 게임의 규칙이 다름”이었다. 동료 타자 자동시뮬도 공용 `resolveShowdownContact()`를 호출한다. 앞으로 정책봇을 추가할 때도 이 함수를 import해서 사용한다. 판정식을 복사해 새로 쓰지 않는다.

## 9. 테스트 체크리스트

### 정적/자동

- `npm test` 통과.
- `npm run build` 통과.
- 시작 덱에 숙련존이 정확히 네 종류인지 확인.
- 타자 실제 경로와 자동시뮬이 모두 `resolveShowdownContact()`를 호출하는지 검색.
- 공격력에 연속 READ 배율을 곱하는 코드가 없는지 확인.
- 게임 시작 시 무조건 `startAiPitch()`를 예약하는 코드가 없는지 확인.

### 직접 플레이

- 새 게임에서 동료 1→2→3번 이후 내 4번 타석에서 자동 진행이 멈춘다.
- 카드가 없는 존을 격자에서 눌러 BASIC SWING이 된다.
- 숙련 READ가 BASIC READ보다 체감상 강하지만 성공이 확정적이지 않다.
- 인접 2존 적중은 `COVERED`이고 DEEP READ가 아니다.
- POWER는 정확한 READ에서 장타 가치가 높고 오독 시 헛스윙이 많다.
- CONTACT는 장타를 희생해 NEAR READ를 살린다.
- CUT은 2스트라이크에서 일부 실패를 파울로 바꾼다.
- ROOKIE의 2스트라이크 중하 Tell을 몇 타석 뒤 발견할 수 있다.
- ADAPTER/FOX가 현재 카드가 아니라 과거 노림에만 반응한다.
- 화면 PUBLIC과 실제 행동의 차이를 숫자 누설 없이 추론할 수 있다.

### 재미 검증 질문

1. 최고 확률이 아닌 존을 의도적으로 고른 순간이 있었나?
2. 적중했을 때 “운이 좋았다”보다 “읽었다”는 감정이 컸나?
3. 실패했을 때 다음 공의 역심리를 생각하게 됐나?
4. POWER 난사가 합리적 지배전략이었나? 그렇다면 수치가 아니라 CQ/PQ 구조부터 재검토한다.

## 10. 다음 작업 우선순위

1. 1타자 vs 1투수 짧은 코어 테스트 모드로 실제 인간 3인 플레이테스트.
2. 고정 시드 정책봇(Random / MaxProb / Pattern / PowerSpam) 추가와 지배전략 회귀 기준 수립.
3. Tell 발견 연출, 히트스톱, 사운드 보강. 신규 카드 대량 추가는 그 이후.

## 11. Visual & Impact Pass 01

2026-08-31에 코어 타석의 시각 계층을 1차 개편했다.

- 데스크톱 전투 화면을 **투수·존(왼쪽) / 카드 베팅(오른쪽)** 2열 구조로 변경했다.
- 존 격자와 카드를 확대하고 전투 전체 폭을 넓혀, 카드 패널이 작은 세로 HUD처럼 보이던 문제를 줄였다.
- 신규 경기장 배경 `assets/stadium-combat-bg-v1.png`을 역할 선택 및 경기 화면에 연결했다.
- `READ`, `DEEP READ`, `COVERED`, `NEAR READ`, `MISREAD`, `CHASE`가 서로 다른 판정 문구로 표시된다.
- DEEP READ에 전용 화면 감광, 중앙 광선, 긴 타이포그래피, 2단 저음을 추가했다.
- POWER가 실제 접촉에 성공했을 때만 저역 타격음을 한 겹 더 재생한다.
- 구형 문구인 “연속 성공 시 타구 강화”를 제거하고 “투수의 반복을 관찰 중”으로 교체했다. 연속 기록은 공격 배율이 아니다.
- 역할 선택 화면의 로고, 선택 카드, 패널 크기와 대비를 강화했다.

다음 시각 작업은 여러 이미지를 한꺼번에 교체하지 않는다. `DEEP READ`, `POWER 정타`, `POWER 실패`, `일반 READ` 네 장면을 실제 플레이 영상으로 비교한 뒤 타자·투수 스프라이트의 해상도와 애니메이션 프레임을 교체한다.

## 12. Sprite & Animation Pass 01 — 120Hz 전투 타임라인

2026-09-01에 구형 소형 base64 스프라이트를 실제 전투 경로에서 교체했다.

- 타자: 스윙 12포즈, 헛스윙 6포즈.
- 투수: 와인드업~팔로스루 12포즈, 삼진 피니시 6포즈.
- 모든 개별 포즈는 투명 512×512 PNG이며 `assets/sprites-v2/frames/`에 있다.
- React는 포즈가 바뀌는 시점에만 이미지 상태를 갱신한다. 위치·회전·스케일·임팩트 밝기는 `requestAnimationFrame`으로 매 디스플레이 갱신마다 보간한다. 120Hz 화면에서는 120fps로 갱신되고, 60Hz 화면에서는 자연스럽게 60fps로 내려간다.
- 스윙은 접촉 키포즈에 짧은 히트스톱을 두며, 본체 뒤에 두 겹의 색수차 잔상을 표시한다.
- AI 와인드업과 릴리스, 유저 투수 릴리스, 삼진 피니시가 모두 동일 타임라인을 사용한다.
- 프레임 재생은 판정 엔진과 분리되어 있다. 애니메이션을 바꾸더라도 CQ/PQ, PUBLIC/TRUE INTENT, TURN GUARD 결과 순서를 건드리면 안 된다.

시각 회귀 확인 항목:

- 타자·투수 이미지의 `naturalWidth/naturalHeight`가 512×512이며 로딩 실패가 없다.
- 헛스윙이 `batter-miss-00`부터 `05`까지 진행하고 대기로 복귀한다.
- 일반/홈런 스윙이 `batter-swing-00`부터 `11`까지 진행한다.
- 투수는 와인드업 `pitcher-pitch-00`~`05` 후 릴리스 `00`~`11`을 재생한다.
- 삼진 시 구형 저해상도 승리 포즈가 아니라 `pitcher-strikeout-00`~`05`를 사용한다.
- 애니메이션 중 잔상은 두 장이며 종료 후 DOM에서 제거된다.
- 브라우저 콘솔 오류가 없고 `npm test`, `npm run build`가 통과한다.

## 13. Impact & Cinematic Pass 02

2026-09-01에 구형 공·배트 클로즈업 중심 컷신을 고해상도 캐릭터 중심 리플레이로 교체했다.

- 결과 컷신은 현재 재생 중인 512px 타자·투수 포즈를 그대로 사용한다. 컷신 진입 때문에 구형 저해상도 캐릭터나 배트 이미지로 되돌아가면 안 된다.
- 접촉 리플레이는 공 진입 → 접촉 히트스톱 → 결과별 타구각을 한 타임라인으로 재생한다.
- `homerun`, `great`, `hit`, `foul`, `out`은 서로 다른 충격파 팔레트를 사용한다.
- 충격파는 중심광, 확장 링, 16개 속도선, 12개 파편으로 구성된다. 외부 이미지가 아니라 CSS/SVG 계층이므로 해상도에 의존하지 않는다.
- 헛스윙은 따뜻한 접촉광 대신 푸른 공기 아크와 통과 궤적을 사용한다.
- 홈런·장타·일반 안타·실패는 서로 다른 카메라 진폭과 감쇠 시간을 사용한다.
- 배너와 컷신 ID는 서로 다른 문자열 접두사를 사용한다. 같은 밀리초에 생성돼도 React key가 충돌하면 안 된다.

브라우저 회귀 확인:

- 접촉 컷신에 `.cinematic-ball` 1개, `.cinematic-character` 1개가 있다.
- 접촉 성공 시 `.impact-burst-debris`가 12개 생성된다.
- 헛스윙 시 충격파 대신 `.cinematic-whiff-arc`가 생성된다.
- 컷신 캐릭터 src가 반드시 `assets/sprites-v2/frames/`를 가리킨다.
- 연출 종료 후 컷신과 파편 DOM이 제거된다.
- 배너와 컷신 동시 생성 시 중복 key 경고가 없어야 한다.

## 14. 자율 개발 루프 운영

2026-09-01에 대화 기억 대신 파일과 Git 커밋으로 이어지는 Windows 자율 루프를 설치했다.

- 운영 본체는 `loop/loop.sh`, Windows 진입점은 `loop/loop.ps1`이다.
- 매 바퀴는 `codex exec --ephemeral`을 새로 실행한다. `resume` 또는 `fork`를 사용하지 않는다.
- `loop/PROMPT.md`를 읽고, `docs/feedback/INBOX.md` → `docs/DESIGN.md` → `docs/STATUS.md` → `HANDOFF.md` 순으로 맥락을 복원한다.
- `loop/env.sh`에 모델, 추론 강도, 작업 사이클 상한, 바퀴 사이 대기, 최대 바퀴 수, 세션 시간 제한, 모든 실행 PATH가 있다.
- `loop/STOP`은 현재 바퀴가 끝난 뒤 정상 종료시킨다.
- 날짜별 JSONL 실행 로그와 마지막 응답은 `logs/`에 저장되며 Git에서 제외된다.
- Windows 작업 스케줄러의 `9ZONE SHOWDOWN Codex Loop` 작업은 로그인 트리거와 실패 시 1분 재시작 정책으로 등록되어 있다. 현재 상태는 **Disabled**다.
- 켜기·끄기·상태 확인은 `loop/task.ps1`의 `Start`, `Stop`, `Status`로 한다.

설치 검증은 `-SmokeTest -MaxRounds 2`로 진행했다. 실행 ID `20260901-120415-2075`의 두 바퀴는 서로 다른 thread ID를 발급받았고 모두 `exit=0`으로 끝났다. 두 세션은 PROMPT와 지정 문서만 읽었으며 소스·문서·Git을 수정하지 않았다. 실제 자율 개발은 시작하지 않았다.

## 15. Shared Engine Extraction

2026-09-02에 `resolveShowdownContact()`를 467 kB 규모의 React 본체에서 `src/game/showdown-engine.js` 순수 모듈로 분리했다.

- `BaseballSim-deck-5.jsx`의 실제 유저 타격 경로와 동료 타자 자동시뮬은 같은 import를 사용한다.
- 판정 수식, 난수 호출 순서, 반환 형태는 변경하지 않았다.
- 테스트는 React와 Tone을 로드하지 않고 공용 엔진을 직접 import한다.
- `pnpm test` 3/3과 `pnpm run build`가 통과했다. 빌드의 500 kB 초과 청크 경고는 기존 알려진 문제로 남아 있다.
- 로컬 서버 실행은 확인했지만 사용 가능한 앱 내 브라우저가 없어 이번 바퀴에서는 스크린샷 QA를 완료하지 못했다.

## 16. One-at-bat Core Playtest Mode

2026-09-02에 인간 플레이테스트를 짧게 반복할 수 있는 타자 전용 `CORE TEST` 진입점을 역할 선택 화면에 추가했다.

- `CORE TEST · 한 타석`은 유저 4번 타순으로 즉시 들어가 동료 자동 타석과 정규 이닝을 기다리지 않는다.
- 한 타석이 볼넷·삼진·아웃·안타 중 하나로 끝나면 자동 진행을 멈추고 재미 검증 네 문항을 표시한다.
- 코어 테스트에서는 랜덤 이벤트, 경험치, 레벨업, 카드 보상을 발생시키지 않는다. CQ/PQ, PUBLIC/TRUE INTENT, 카드 선택 규칙은 정규 타자 경기와 같다.
- 네 문항을 모두 답해야 결과를 저장할 수 있다. 결과에는 답변, 선택 메모, 마지막 플레이, 본 투구 기록이 포함된다.
- 결과는 브라우저 `localStorage`의 `9zone-core-test-results-v1`에 누적되며 역할 선택 화면에서 현재 건수와 JSON 다운로드를 확인할 수 있다.
- `다음 플레이어`는 상태를 초기화하고 새 한 타석의 첫 투구를 명시적으로 예약한다. 정규 경기의 TURN GUARD는 그대로 유지한다.

검증은 `pnpm test` 3/3, `pnpm run build`, `git diff --check`, 로컬 서버 HTTP 200까지 통과했다. 브라우저 런타임에 사용 가능한 브라우저가 없어 스크린샷 및 클릭 회귀는 완료하지 못했다. 다음 세션은 역할 선택 → 코어 테스트 즉시 진입 → 타석 종료 → 네 문항 저장 → 다음 플레이어 재시작 → JSON 다운로드를 실제 브라우저에서 먼저 확인한 뒤 인간 3인 테스트를 진행한다.
