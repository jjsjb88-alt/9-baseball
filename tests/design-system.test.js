import {describe, it} from 'vitest';

describe('V12 P0-4 design-system baseline scaffold', () => {
  it('records the current CSS source metrics without enforcing migration thresholds', () => {
    throw new Error('P0-4 RED: replace this marker with the non-blocking baseline recorder');
  });
});
